# CyberScripts — Tool Index

*Last updated: 2026-07-30 — 215 tools*

## BlueTeam

### Deception
- **Create a Deceptive User in AD** (PowerShell) — Creates a honeypot user account in Active Directory to detect lateral movement and credential attacks.

### Detection
- **ACR Stealer Browser Vault Access & Token Exfiltration Detector** (Python) *(draft)* — Scans plain-text Sysmon or EDR log exports for ACR Stealer's characteristic browser credential vault file access and authentication token exfiltration patterns, labeling each match with a MITRE ATT&CK technique, kill-chain stage, and severity. Targets the ClickFix → ACR Stealer kill chain by correlating non-browser process access to Chrome, Firefox, and Edge credential stores with subsequent outbound network indicators.
- **AI API Cross-Tenant Leak Detector** (Python) *(draft)* — Scans plain-text AI platform API access logs for cross-tenant data leakage patterns modeled on known Dify vulnerabilities, including unauthorized document preview bypass and unauthenticated internal API exposure. Emits NDJSON alerts to stdout when tenant boundary violations are detected across dataset, document, and console API endpoints.
- **AI Agent Attack Chain Behavioral Detector** (Python) *(draft)* — Scans plain-text API gateway, LLM orchestration, or cloud function execution logs for behavioral signatures of autonomous AI agent attack chains across three kill-chain stages — reconnaissance via automated API enumeration and metadata harvesting, lateral movement through agent-mediated credential relay and cross-service tool pivoting, and impact via LLM-orchestrated bulk file access or encryption-stage KMS abuse. Distinguishes agentic attack sessions from human-operated or scripted traffic by analyzing inter-request timing regularity, burst-pattern cadence, and sequential tool-call ordering that are statistically inconsistent with human interaction.
- **AI Agent Financial Transaction Prompt Injection Detector** (Python) *(draft)* — Scans plain-text AI agent session logs for third-party web content that embeds prompt-injection directives causally linked to unauthorized financial transaction actions within the same session. Pairs each finding with the kill-chain stage (Injection/Trigger/Execution), the responsible injection phrase, and a one-line mitigation to help defenders tune AI agent trust boundaries.
- **AI Agent Post-Exploitation Egress and Tool-Call Sequencing Detector** (Python) *(draft)* — Scans plain-text forward proxy access logs or LLM orchestration framework exports for behavioral signatures of autonomous AI agent post-exploitation activity — unattended LLM API egress to known provider endpoints, machine-regular beaconing cadence indicative of YOLO-mode agent loops, and sequential tool-call invocation timing statistically inconsistent with human-in-the-loop operation. Modeled on the Hermes autonomous agent incident targeting the Thai Finance Ministry, where agentic tool-call sequencing and unattended LLM provider egress were the primary observable behavioral artifacts in proxy telemetry.
- **AI Asset Integrity Monitor — EncForge-Style Ransomware Pre-Encryption Detector** (Python) *(draft)* — Scans plain-text auditd, inotifywait, or file-access log exports for behavioral indicators of EncForge-style ransomware targeting AI infrastructure assets — model checkpoints, vector database files, and training dataset directories — by matching bulk-read staging, anomalous rename cascades, and mass-write sequences against configurable AI asset extension profiles before encryption completes. Each match is labeled with the asset class targeted (ModelCheckpoint/VectorDB/TrainingData), severity, and the triggering process and file-path context extracted from the log line.
- **AI Coding Assistant Prompt Injection Detector** (Python) *(draft)* — Scans plain-text IDE and AI coding assistant activity logs for prompt-injection signatures that mirror the Amazon Q CVE attack pattern, flagging attempts to coerce the assistant into accessing and exfiltrating cloud credentials via malicious repository content. Correlates injection trigger phrases with downstream credential-access and outbound-transfer indicators within a sliding line window to surface high-confidence exfiltration chains.
- **AI Gateway CloudTrail Abuse & Cryptomining Egress Detector** (Python) *(draft)* — Scans plain-text CloudTrail or LLM gateway log exports for AI-gateway abuse patterns across three kill-chain stages — anomalous model invocations where unauthorized model IDs, excessive token consumption, or off-hours Bedrock/SageMaker API calls indicate credential compromise, lateral IAM calls where AI service roles pivot to enumerate identities or escalate privileges via adjacent AWS services, and cryptomining egress signatures where stratum-protocol patterns, known mining pool domains, or GPU instance reservation sequences appear following unauthorized invocation events. Targets the emerging threat surface where compromised or misconfigured AI gateway credentials are weaponized for unauthorized inference, cloud account takeover via IAM pivot, and cryptomining using provisioned GPU capacity.
- **AI Pipeline Prompt Injection Detector** (Python) *(draft)* — Scans plain-text logs from AI-assisted malware analysis pipelines for adversarial natural language payloads that attempt to hijack, abort, or corrupt the analysis by targeting the underlying LLM's instruction-following behavior. Emits flagged lines with severity scores, matched signatures, and contextual snippets to stdout so analysts can identify evasion-aware malware before it poisons automated verdicts.
- **AI Recon Pattern Detector** (Python) *(draft)* — Analyzes plain text auth/syslog entries to identify behavioral signatures of LLM-driven lateral movement, such as systematic sequential probing and structured host enumeration chains. Outputs scored alert lines to stdout for use in developing detection rules against autonomous AI-reasoned reconnaissance.
- **AI SaaS Cross-Tenant Session Isolation & WriteOut Leak Detector** (Python) *(draft)* — Scans plain-text AI API session logs for cross-tenant context contamination where one tenant's system prompt fragments, data markers, or proprietary context appear in another tenant's response payloads — the WriteOut vulnerability pattern documented in AI SaaS multi-tenant inference platforms. Correlates tenant-tagged log entries across three leak stages: Contamination where foreign-tenant markers appear in a session's context window, Exfiltration where leaked content surfaces in the assistant response payload, and Persistence where the contaminated context recurs across consecutive turns indicating context-window absorption.
- **AI Toolchain LOLBin Abuse Detector** (Python) *(draft)* — Scans plain-text process execution or shell audit logs for legitimate AI toolchain binaries (python, pip, jupyter, ollama, huggingface-cli) being abused as living-off-the-land binaries across two kill-chain stages: reconnaissance and staging via AI-native download and enumeration channels, and impact via credential harvesting, C2 beaconing, or persistence established through trusted AI runtime processes. Uses Sandworm_Mode behavioral signatures — patterns derived from AI workflow activity that appears benign in isolation but indicates malicious intent when combined with environmental context, argument sequences, and process-tree position.
- **AI-Brand Malvertising & SectopRAT Staging Detector** (Python) *(draft)* — Parses plain-text proxy or DNS resolver logs for malvertising campaign indicators targeting AI-brand impersonation — detecting typosquat domains mimicking Claude, ChatGPT, Midjourney, Gemini, and Copilot, alongside Bing ad click referrer chains that funnel victims to lookalike installers. Each hit is labeled with a kill-chain stage and paired with a one-line analyst action to support rapid triage of SectopRAT-linked AI-lure malvertising incidents.
- **AgentForge Sub-Agent Spawn Chain Detector** (Python) *(draft)* — Scans enterprise AI agent session logs for behavioral signatures of AgentForger-style attacks where injected prompts silently coerce the primary agent into spawning unauthorized sub-agents. Correlates injection markers, spawn directives, and covert execution indicators across the same session to surface complete attack chains with kill-chain stage labels and mitigations.
- **AiTM PhaaS Token Harvest & M365 OAuth Abuse Detector** (Python) *(draft)* — Scans plain-text M365 or Azure AD sign-in log exports for adversary-in-the-middle token harvesting patterns associated with ARToken/EvilTokens PhaaS infrastructure across three kill-chain stages — AiTM staging where reverse-proxy indicators and known PhaaS redirect URIs appear in OAuth flows, token harvesting where authorization codes or session tokens are intercepted mid-flow, and replay abuse where captured tokens are reused from mismatched IP addresses or user-agents indicating post-harvest account takeover. Targets the PhaaS ecosystem documented in BleepingComputer reporting on EvilTokens and ARToken kits that bypass MFA by proxying legitimate M365 OAuth sessions in real time.
- **Anubis Ransomware Kill Chain Detector (CVE-2025-5777 / BYOVD / Supply Chain)** (Python) *(draft)* — Scans plain-text log exports for indicators of the Anubis ransomware kill chain across three stages: Citrix Bleed 2 (CVE-2025-5777) session-token extraction against NetScaler ADC/Gateway endpoints, BYOVD kernel driver abuse for defense evasion, and supply chain credential harvesting targeting CI/CD pipelines and package-registry auth stores. Each match is labeled by kill-chain stage with severity, source identifier, and the verbatim log line that triggered it.
- **Autonomous AI Agent Post-Exploitation Behavioral Detector** (Python) *(draft)* — Scans plain-text auditd, syslog, or process execution log exports for behavioral signatures of autonomous AI agent post-exploitation activity — unattended command execution with machine-precise timing, lateral movement via SSH pivoting and credential relay, and persistence deployment absent any interactive TTY or human-confirmation pause. Distinguishes agentic post-exploitation sessions from human-operated intrusions by detecting non-TTY session context, sub-human inter-command cadence variance, and sequential kill-chain progression statistically inconsistent with human decision latency.
- **Avalon Multi-Stage Phishing Chain & Lateral Movement Detector** (Python) *(draft)* — Scans plain-text proxy, SMTP gateway, or endpoint log exports for indicators of Avalon's documented multi-stage phishing intrusion chain across three kill-chain stages — initial spearphishing delivery and malicious document staging, payload execution and C2 beacon establishment via scripting engine abuse, and lateral movement through credential replay and internal service pivoting. Each finding is labeled with the corresponding MITRE ATT&CK technique ID to support triage and purple-team coverage gap analysis.
- **BMC/IPMI Exposure & Offline NTLM Hash Cracking Sentinel** (Python) *(draft)* — Scans plain-text firewall or authentication logs for two paired threat signals across the credential-access kill chain: internet-facing BMC/IPMI controller enumeration (UDP/TCP port 623, RMCP probe fingerprints, Shodan/Censys scanner IP ranges, and BMC management-web port sweeps against your ASN) and offline NTLM hash cracking precursor patterns (NTLMv1 downgrade negotiations, multi-source hash relay indicators, and rapid failed-authentication bursts signaling a captured hash under test). Each signal is labeled with a MITRE ATT&CK technique ID and kill-chain stage.
- **BeyondTrust RS/PRA Unauthenticated Auth Exploit Detector (CVE-2024-12356)** (Python) *(draft)* — Scans plain-text web access log exports for unauthenticated exploitation indicators targeting BeyondTrust Remote Support and Privileged Remote Access authentication endpoints, covering CVE-2024-12356 command-injection probes, auth-bypass reconnaissance, and post-exploitation staging patterns. Each match is labeled with a MITRE ATT&CK technique ID, kill-chain stage, severity, and the verbatim log line that triggered it.
- **BitM Phish Detector** (Python) *(draft)* — Scans plain-text web proxy or access logs for browser-in-the-middle phishing infrastructure indicators, including reverse-proxy TLS header anomalies, session-cookie relay signatures, and Bluekit-style hostname clustering patterns. Emits timestamped findings to stdout with confidence scoring and indicator type tagging.
- **CDN Hash Sentinel** (Python) *(draft)* — Simulates a CDN-level supply chain attack by generating a fake plugin update log with an injected hash mismatch, then scans the log to detect unexpected asset hash changes that indicate possible JavaScript tampering. Demonstrates how blue teams can monitor CDN asset integrity to catch compromised plugin update channels.
- **CUCM CVE-2026-20230 Triage Scanner** (Python) *(draft)* — Parses Cisco Unified Communications Manager plain-text audit and access logs for SSRF trigger patterns and privilege-escalation sequences linked to CVE-2026-20230. Emits severity-rated findings with matched line context to stdout for rapid analyst triage within the sub-24-hour weaponization window.
- **CVE-2026-10702 Browser JIT Exploitation Detector** (Python) *(draft)* — Scans plain-text web proxy access logs or browser telemetry exports for network-level and renderer-process indicators of CVE-2026-10702 JIT compiler exploitation — covering heap-spray JS delivery patterns, renderer-process crash telemetry signatures consistent with JIT RCE, and post-exploitation C2 callback patterns originating from the compromised renderer context. Flags three kill-chain phases with MITRE ATT&CK technique IDs without requiring endpoint agent telemetry.
- **CVE-2026-20230 Cisco UCM Triage Scanner** (Python) *(draft)* — Scans plain-text logs for indicators of active exploitation of CVE-2026-20230 in Cisco Unified Communications Manager using pattern matching against known attack signatures. Outputs a structured triage report with matched IOCs, severity assessment, and patch verification guidance to stdout.
- **Certighost AD CS Privilege Escalation Detector** (Python) *(draft)* — Scans plain-text AD CS Certificate Services log exports for Certighost-style Active Directory Certificate Services privilege escalation indicators across three kill-chain stages: template enumeration and discovery, cross-account certificate enrollment with unexpected Subject Alternative Name values, and high-value target certificate issuance anomalies. Each finding is labeled with a kill-chain stage, Sigma rule ID, MITRE ATT&CK technique, severity, and a one-line mitigation; the final report appends a prioritized AD CS hardening checklist scoped to the highest-severity stage observed.
- **Check Point VPN Qilin Sigma Rule Generator** (Python) *(draft)* — Generates Sigma detection rules for the Check Point VPN auth-bypass exploitation chain (CVE-2024-24919) used by Qilin ransomware affiliates, mapping each attack stage to MITRE ATT&CK. Accepts plain-text VPN/auth logs and outputs ready-to-use Sigma YAML rules to stdout.
- **CitrixBleed NetScaler Memory-Disclosure & Session-Hijack Detector** (Python) *(draft)* — Scans plain-text HTTP access log exports from NetScaler ADC/Gateway appliances for CVE-2023-4966 CitrixBleed memory-disclosure attack signatures across three kill-chain stages — initial exploit attempts against vulnerable authentication endpoints producing anomalously oversized responses, session-hijack activity using stolen in-memory tokens, and post-exploitation Anubis ransomware staging and BYOVD delivery indicators tied to the CVE-2025-5777 supply-chain intrusion chain. Each finding is labeled with a MITRE ATT&CK technique ID to support immediate NetScaler defender triage.
- **ClickFix Loader Detector** (Python) *(draft)* — Scans a plaintext log file — EDR telemetry, PowerShell transcript, or Windows Security audit export — for ClickFix social-engineering loader indicators across three kill-chain stages: clipboard-lure delivery, LOLBin-assisted remote payload fetch, and stager execution. Emits one labeled alert line per match with stage and severity, enabling SOC analysts to triage potential ClickFix infections without requiring a SIEM or binary parser.
- **Commvault Ransomware Pre-Staging Detector** (Python) *(draft)* — Analyzes plain-text Commvault log exports for ransomware pre-staging indicators including anomalous API calls, credential enumeration patterns, and job-schedule tampering. Helps blue team analysts triage potential ransomware intrusions targeting backup infrastructure before encryption begins.
- **Contagious Interview SVG Steganography & Four-Stage Chain Detector** (Python) *(draft)* — Scans plain-text endpoint log exports or raw SVG artifact files for indicators of Contagious Interview's SVG steganography delivery chain across four kill-chain stages — SVG payload staging and base64 blob concealment, JavaScript decoder and dropper execution, BeaverTail infostealer credential harvesting, and InvisibleFerret Python backdoor C2 establishment. Each finding is labeled with the corresponding MITRE ATT&CK technique ID to support triage and purple-team coverage gap analysis.
- **Container Escape & Privesc Detector** (Python) *(draft)* — Parses Linux audit and syslog plaintext output to detect exploitation signatures associated with kernel privilege-escalation and container breakout techniques (e.g., nf_tables UAF, runc/OverlayFS escapes, capability abuse). Includes a lab mode that emits synthetic exploit event sequences to validate detection coverage without a live environment.
- **CrashStealer macOS Crash Reporter Masquerade Detector** (Python) *(draft)* — Scans macOS unified log exports for post-Gatekeeper execution anomalies matching the CrashStealer notarized-dropper pattern, detecting processes that impersonate Apple crash reporting utilities (CrashReporter, ReportCrash, crashpad_handler) while running from non-system paths, accessing credential stores, or staging secondary payloads. Covers three kill-chain stages — crash reporter name masquerade, credential store access from spoofed process lineage, and notarized dropper payload staging — each labeled with a MITRE ATT&CK technique ID.
- **DetectIncommingConnection** (Python) — Monitors and alerts on new incoming network connections in real time.
- **EDR Probe Detector** (Python) *(draft)* — Analyzes plain text security logs to identify automated AI-assisted EDR evasion probing patterns such as rapid enumeration, timing anomalies, and systematic API call sequences. Generates actionable Sigma and YARA rules to detect these reconnaissance behaviors before any payload is deployed.
- **EncForge/JADEPUFFER Langflow RCE-to-Ransomware Kill-Chain Detector** (Python) *(draft)* — Scans plain-text Langflow execution logs or co-located web server access logs for the ENCFORGE/JADEPUFFER ransomware kill chain — initial access via Langflow RCE exploitation of code-execution node types (PythonREPL, BashTool, CodeExecutor), post-exploitation AI asset staging, and pre-encryption bulk-read or double-extension rename sequences targeting model weights and vector index files. Each match is labeled with kill-chain phase (InitialAccess/Staging/PreEncryption), severity, and the matched operator TTP extracted from the log line context.
- **Entra ID MFA/Passkey Enrollment Vishing Detector (O-UNC-066)** (Python) *(draft)* — Parses plain-text Entra ID / Microsoft Sentinel audit log exports to flag anomalous passkey or MFA enrollment events preceded by atypical sign-in geography, new ASN, or off-hours timing within a configurable look-back window, matching the O-UNC-066 vishing pattern where threat actors social-engineer users into enrolling attacker-controlled authenticators. Each finding is correlated to its triggering sign-in event, labeled with an anomaly reason and severity, and paired with a Sentinel KQL snippet for immediate production deployment.
- **Exposed AI Service Endpoint Detector** (Python) *(draft)* — Analyzes plain-text web server or reverse-proxy access logs for request patterns matching exposed AI service endpoints (Ollama, ComfyUI, Langflow, Open WebUI, Gradio), flagging active inference API access, service enumeration probes, and scanner burst activity as organizational exposure evidence. Targets the unauthenticated AI service attack surface documented in recent Shodan and Censys research showing tens of thousands of publicly reachable Ollama and Gradio instances serving inference requests without authentication.
- **Fastjson CVE-2026-16723 RCE WAF & Sigma Detection Scanner** (Python) *(draft)* — Scans plain-text combined-format HTTP access logs for CVE-2026-16723 Fastjson 1.x remote code execution exploitation signatures targeting Spring Boot applications across three kill-chain stages: auto-type probe and fingerprinting, active JNDI injection payload delivery via @type gadget chains, and post-exploitation callback confirmation. Built for teams that cannot immediately migrate off Fastjson 1.x, it emits Sigma-compatible rule-labeled alerts with per-stage severity and a Spring Boot-specific triage checklist covering ParserConfig safeMode and denyList hardening.
- **Flying Eagle RAT C2 Hunter & IOC Feed Generator** (Python) *(draft)* — Scans plain-text TLS handshake logs or HTTP scan output for Flying Eagle RAT C2 infrastructure by matching bundled certificate fingerprints and panel HTTP signatures from Hunt.io's research, emitting per-match detection alerts labeled with MITRE ATT&CK techniques. Closes each run by auto-generating a structured IOC feed block containing all matched C2 indicators — hostnames, IPs, cert hashes, and panel strings — ready for ingestion into a SIEM or threat-intel platform.
- **Forg365 Antibot Bypass and Post-Compromise Forwarding Detector** (Python) *(draft)* — Parses plain-text M365 Unified Audit Log exports for Forg365 PhaaS antibot-bypass indicators present during OAuth device-code delivery and for post-compromise mailbox manipulation events that follow successful session-cookie or device-code token theft. Extends kill-chain coverage beyond the initial compromise window to the data-exfiltration staging phase where threat actors establish persistent external email forwarding and inbox rules.
- **FortiBleed Post-Exploitation Triage Scanner** (Python) *(draft)* — Scans plain-text web server or proxy access log exports for post-exploitation indicators following FortiBleed attacks — Fortinet credential and configuration exposure exploits — covering the full intrusion chain through Nextcloud zero-day abuse and Inc/Lynx ransomware pre-deployment staging. Detects three kill-chain stages — FortiGate/FortiManager exploitation and credential-exposure probes, Nextcloud unauthorized API access and bulk file-staging patterns, and Inc/Lynx ransomware lateral-movement and data-aggregation behaviors — each finding labeled with a MITRE ATT&CK technique ID.
- **FortiBleed Triage Scanner** (Python) *(draft)* — Scans plain-text FortiGate logs for exploitation indicators tied to the FortiBleed vulnerability cluster (CVE-2024-21762, CVE-2023-27997, CVE-2024-23113), correlating suspicious SSL-VPN requests, auth-bypass patterns, and post-exploitation beaconing. Outputs a prioritized hit list with source IPs, matched CVE tags, and a remediation checklist for triaging exposed devices.
- **FortiGate VPN Credential Dump Reuse & Inc/Lynx Staging Detector** (Python) *(draft)* — Scans plain-text FortiGate VPN authentication log exports for credential dump reuse patterns — where accounts exposed via FortiBleed are replayed against active VPN gateways — correlating authentication anomalies such as credential-stuffing bursts, off-hours logins, and geolocation shifts with Inc/Lynx pre-ransomware staging behaviors. Covers three kill-chain stages — FortiBleed-exposed credential replay and authentication-success-after-failure sequences, VPN authentication anomaly clusters tied to known threat-actor source ranges, and Inc/Lynx lateral-movement and session-persistence indicators — each finding labeled with a MITRE ATT&CK technique ID.
- **Gamaredon TTP Log Scanner** (Python) *(draft)* — Scans plain-text logs against a curated ruleset of Gamaredon (Shuckworm/Armageddon) TTPs, targeting their updated malware-loading chains — PowerShell loaders, LOLBin abuse, LNK/HTA droppers — and infrastructure-obfuscation patterns such as dynamic-DNS C2, Telegram bot channels, and fast-flux indicators. Emits per-match findings with TTP label, severity, and source line to stdout for immediate triage or pipeline consumption.
- **Ghost Account GitHub Org Enumeration Detector** (Python) *(draft)* — Analyzes GitHub API access logs to identify ghost and newly-created accounts performing rapid enumeration of organization members and repositories by correlating account-age indicators, sequential pagination patterns, and cross-endpoint request bursts from the same actor within compressed time windows. Covers three signal classes across the enumeration kill chain: new-account burst probing, automated pagination sweeps, and same-actor cross-endpoint org recon campaigns.
- **Ghost Phishing Client-Side Decryption Detector** (Python) *(draft)* — Parses plain-text proxy log exports or HTTP response body dumps for indicators of client-side-decrypted ghost phishing pages that evade static email scanners by delivering encrypted HTML payloads decrypted in-browser via JavaScript. Each finding is labeled with a delivery-stage tag (EncryptedDelivery, DecryptionLogic, or KeyExtraction) and paired with a one-line analyst note to accelerate triage of this evasion class.
- **GhostLock CVE-2026-43499 Privilege Escalation & Container Escape Detector** (Python) *(draft)* — Scans plain-text auditd or Falco log exports for behavioral signatures of the GhostLock (CVE-2026-43499) exploit chain across two kill-chain stages: privilege escalation via user-namespace abuse and capability manipulation, and container escape through cgroup release-agent writes, /proc/sys/kernel/core_pattern tampering, or host-namespace breakout syscall sequences. Each match is labeled by stage with severity, the triggering process or container context, and the verbatim log line.
- **GigaWiper Destructive Mode Pre-Execution Detector** (Python) *(draft)* — Scans plain-text auditd, Sysmon CSV, or EDR text log exports for behavioral indicators that a GigaWiper operator is actively selecting among three destructive payload modes — MBR wipe, fake ransomware note drop, or spyware beacon — before full payload execution, matching mode-specific pre-flight access patterns such as raw disk device probing, ransom-note template staging, and timed beacon interval calibration. Each match is labeled with the candidate destructive mode, severity, and the triggering process and file-path context extracted from the log line.
- **GitHub Org Enumeration Abuse Monitor** (Python) *(draft)* — Analyzes GitHub API access logs for dormant-account abuse and org enumeration campaigns by flagging unauthenticated membership queries, anomalous per-IP request rate spikes, and known scraper user-agent signatures targeting org member and team endpoints. Covers three signal classes across the enumeration kill chain: credential-free probing, automated tooling signatures, and volume-based scraping patterns.
- **Gitea Docker Registry Auth Bypass and Admin Impersonation Detector** (Python) *(draft)* — Scans Gitea or reverse-proxy access log exports for Docker registry authentication bypass indicators and admin-impersonation patterns, flagging unauthenticated v2 API probes, anomalous bearer-token grants on privileged endpoints, and repository pulls from container-network source addresses consistent with post-bypass exfiltration. Covers three kill-chain stages — Docker auth endpoint reconnaissance, admin credential impersonation via malformed Authorization headers, and unexpected repository access from container-hosted instances.
- **Gravity SMTP Secret Exposure Scanner** (Python) *(draft)* — Scans plaintext web server access logs for request patterns indicative of unauthenticated exploitation of the Gravity SMTP plugin's info disclosure vulnerability, matching against a Sigma-style detection rule. Outputs timestamped findings to stdout with severity, matched indicators, and source IPs to support triage of active exploitation attempts against WordPress deployments.
- **Headless Browser C2 Proxy & msaRAT Pre-Encryptor Detector** (Python) *(draft)* — Scans plain-text Windows process-creation log exports for indicators of browser-as-C2-proxy implants across three kill-chain stages — headless Chrome or Edge spawned by non-user service processes, DevTools remote-debugging port abuse for C2 relay, and msaRAT-characteristic pre-encryptor behaviors including registry persistence, credential access, and shadow copy enumeration. Each finding is labeled with the corresponding MITRE ATT&CK technique ID to support triage and pre-ransomware containment decisions.
- **Hermes Autonomous Agent Lateral Movement TTP Detector and Sigma Rule Generator** (Python) *(draft)* — Scans plain-text endpoint, authentication, or Windows Security log exports for behavioral signatures of the Hermes autonomous agent operating in YOLO mode during lateral movement — credential access via automated enumeration bursts, host discovery sequencing, and remote service pivoting patterns that distinguish agentic lateral movement from human-paced intrusion activity. For each detected TTP cluster, emits a Sigma rule stub mapped to the corresponding MITRE ATT&CK technique ID, enabling purple-team coverage validation and SIEM rule authoring without requiring a separate detection-engineering step.
- **HollowGraph M365 Calendar C2 Dead-Drop Detector** (Python) *(draft)* — Parses M365 Unified Audit Log exports for indicators of HollowGraph-style Calendar-based command-and-control dead-drop behavior, flagging encoded event content, automated polling cadence anomalies, and suspicious GraphAPI calendar access patterns. Maps each hit to MITRE ATT&CK T1102.001 (Dead Drop Resolver), emits a paired Sentinel KQL rule snippet, and outputs a prioritized C2-severance checklist.
- **Iranian ICS TTP Detector for Siemens, Schneider, and Rockwell PLCs** (Python) *(draft)* — Scans plain-text OT network traffic log exports for indicators of known Iranian threat actor TTPs targeting Siemens, Schneider Electric, and Rockwell Automation PLCs, covering three kill-chain stages — PLC reconnaissance via industrial protocol enumeration, unauthorized command injection and ladder logic manipulation, and persistence through repeated authentication abuse or external C2 beaconing. Each finding is labeled with the corresponding MITRE ATT&CK for ICS technique ID drawn from CISA ICS-CERT advisories on CyberAv3ngers and affiliated IRGC-linked ICS campaigns.
- **LLM-Integrated Offensive Tool Behavioral Detector** (Python) *(draft)* — Scans plain-text API gateway, web proxy, or endpoint execution logs for behavioral signatures of LLM-integrated offensive tooling across three artifact classes — prompt injection payloads embedded in HTTP request bodies or URI parameters, API call patterns indicating LLM model enumeration, token-stuffing, or orchestration framework fingerprints, and jailbreak scaffolding signatures such as system-prompt override sequences, roleplay escalation templates, and DAN-style instruction injections. Distinguishes opportunistic LLM abuse from authorized model access by correlating injection artifact co-occurrence with anomalous API path targeting and multi-turn scaffolding sequences within per-session windows.
- **Langflow Agentic Tool-Call Abuse & Lateral Movement Detector** (Python) *(draft)* — Scans plain-text Langflow or LLM orchestration framework execution logs for agentic attack patterns across three kill-chain stages — tool-call abuse where LLM reasoning chains invoke high-risk capabilities such as shell execution or credential file access, lateral movement where agents pivot across flows or memory stores via injected reasoning steps, and exfiltration via LLM-mediated outbound tool invocations or prompt-encoded data egress. Targets the emerging attack surface of autonomous agent frameworks where compromised or injected reasoning steps can weaponize legitimate tool nodes against internal infrastructure.
- **Langflow CVE-2026-5027 Triage Scanner** (Python) *(draft)* — Detects exploitation attempts and post-exploitation artifacts of the Langflow unauthenticated file-write RCE vulnerability (CVE-2026-5027). Scans plain-text log files for suspicious endpoint hits, file-write payloads, and known post-exploitation indicators on AI development hosts.
- **LiteLLM SSRF-to-RCE Detector** (Python) *(draft)* — Scans LiteLLM and AI gateway plain-text logs for SSRF-to-RCE exploit chain indicators using regex-based IOC matching against known attack patterns targeting the /config/update endpoint, internal callback URIs, and metadata service probes. Outputs timestamped flagged lines with severity tags and a prioritized hardening checklist for LiteLLM deployments.
- **LongLeash / DogLeash / JarLeash SOHO Router C2 & Persistence Detector** (Python) *(draft)* — Scans plain-text SOHO router syslog exports for IOCs and behavioral signatures tied to the LongLeash, DogLeash, and JarLeash implant families across three kill-chain stages — initial C2 beacon establishment, sustained command-and-control communication, and router persistence mechanism indicators. Each finding is labeled with a MITRE ATT&CK technique ID to support immediate network defender triage on compromised SOHO infrastructure.
- **MCP Server Endpoint & AI Credential Path Scan Detector** (Python) *(draft)* — Analyzes plain-text web server access logs for reconnaissance activity targeting Model Context Protocol server endpoints and exposed AI assistant credential paths, correlating MCP-specific probe patterns with credential enumeration sequences and high-frequency burst signatures. Covers three signal classes across the discovery kill chain: MCP endpoint probing, AI credential path enumeration, and scanner burst activity — each labeled with a MITRE ATT&CK technique ID.
- **MCP Server Runtime Policy Violation Detector** (Python) *(draft)* — Scans MCP server session logs for runtime policy violations where tools invoke capabilities outside their declared permission scope or embed prompt-injection directives in tool responses returned to the LLM host. Pairs each finding with the violation category (PermissionAbuse/InjectionVector/ScopeEscalation), the responsible tool name, and a one-line mitigation drawn from large-scale MCP permission-abuse study findings.
- **MITRE ATT&CK Log Threat Mapper** (Python) *(draft)* — Scans plain-text log exports from common sources — syslog, Apache/Nginx access logs, SSH auth.log, and Windows security event text exports — matching entries against bundled MITRE ATT&CK technique signatures across nine tactic categories and emitting plain-English threat summaries tuned for tier-1 SOC analysts who need actionable context without requiring ATT&CK framework expertise. Each finding includes a human-readable explanation of what the attacker behavior means, the relevant technique ID and tactic name, severity rating, and source identifier to accelerate triage and escalation decisions.
- **Miasma Rule Generator** (Python) *(draft)* — Scans plain-text logs for Miasma malware IOCs and behavioral signatures tied to its credential-harvesting and supply-chain delivery mechanics. Emits ready-to-deploy YARA or Sigma rules to stdout based on matched evidence.
- **NetScaler Memory-Disclosure IOC Feed & Session-Cookie Exfiltration Detector** (Python) *(draft)* — Scans plain-text NetScaler ADC/Gateway HTTP access log exports for active exploitation of new NetScaler memory-disclosure CVEs by matching PoC-derived request patterns that trigger out-of-bounds memory reads on vulnerable authentication endpoints and detecting anomalous NSC session-cookie exfiltration sequences where harvested in-memory tokens surface in URI query strings or Referer fields rather than Cookie headers. Each finding is labeled with a kill-chain stage and MITRE ATT&CK technique ID to accelerate NetScaler defender triage against rapid exploitation campaigns.
- **Netlogon RCE Detector** (Python) *(draft)* — Detects CVE-2026-41089 exploitation attempts by scanning plain-text Windows Security event logs for suspicious Event ID 4742/5805 patterns and anomalous Netlogon RPC signatures. Alerts on credential resets, unauthorized secure channel establishment, and RPC anomalies indicative of privilege escalation via Netlogon.
- **Oracle EBS CVE-2026-46817 Exploitation Detector** (Python) *(draft)* — Scans plain-text Oracle HTTP Server or Apache combined-format access log exports for active exploitation of CVE-2026-46817 against Oracle E-Business Suite, prioritized for financial-sector SOC teams. Detects three kill-chain stages — reconnaissance probing of vulnerable EBS servlet endpoints, authentication-bypass exploitation payloads in request URIs, and post-exploitation IOCs including attacker IPs and exfiltration domains observed in financial-sector campaigns.
- **PamStealer macOS PAM Abuse & AppleScript Detector** (Python) *(draft)* — Scans macOS unified log exports for PamStealer campaign indicators, detecting PAM module injection into /etc/pam.d/ authorization stacks and compiled AppleScript execution via osascript or osacompile launched from unsigned or path-impersonating binaries. Covers three kill-chain stages — PAM module staging, privileged credential interception, and AppleScript-based execution or exfiltration — each labeled with a MITRE ATT&CK technique ID.
- **SEO Poison DNS Detector** (Python) *(draft)* — Parses plain-text DNS or proxy logs to flag domains matching indicators associated with SEO-poisoned fake open-source package sites delivering Remus Stealer and SessionGate via traffic distribution systems. Outputs timestamped alerts to stdout and optionally appends flagged IPs to a blocklist file compatible with DetectIncomingConnectionandBlockIP.
- **ShareFile Storage Zone Controller Exploitation Detector** (Python) *(draft)* — Scans plain-text IIS or Apache access log exports for indicators of ShareFile Storage Zone Controller exploitation — covering CVE-2023-24489 unauthenticated RCE via malformed upload requests, post-exploitation webshell staging on the storage zone host, and C2 callback patterns from the compromised controller. Flags three attack phases with MITRE ATT&CK technique IDs and emits a host-isolation runbook block on any high-severity finding.
- **SharePoint CVE-2026-45659 RCE Post-Exploitation Detector** (Python) *(draft)* — Scans plain-text IIS W3C or Apache combined-format access log exports for post-exploitation indicators following successful CVE-2026-45659 SharePoint RCE, a CISA KEV-listed critical vulnerability under active exploitation. Detects three kill-chain stages — initial RCE trigger attempts against vulnerable SharePoint server-side processing endpoints, web shell deployment and interactive access patterns in SharePoint layout and catalog paths, and post-exploitation activity including credential staging URI patterns, lateral movement beacons, and known campaign C2 IOCs.
- **SharePoint Three-CVE SIEM Detection Rule Set** (Python) *(draft)* — Scans plain-text IIS W3C access log exports for exploitation and post-exploitation indicators across three CISA KEV-listed SharePoint CVEs actively exploited per BleepingComputer — CVE-2026-45659 (RCE via server-side processing endpoints), CVE-2026-37986 (elevation-of-privilege via authentication token forgery), and CVE-2026-33078 (SSRF enabling internal network reconnaissance). Each alert is labeled with CVE identifier, kill-chain stage, and MITRE ATT&CK technique ID to accelerate SIEM rule authoring and purple-team coverage gap analysis.
- **SimpleHelp CVE-2026-48558 & Djinn Cloud/AI Credential Exfil Detector** (Python) *(draft)* — Scans plain-text HTTP access or SIEM log exports for active exploitation of CVE-2026-48558 against SimpleHelp remote-support instances combined with credential-exfiltration patterns exhibited by the Djinn stealer, which specifically targets cloud-provider credential files and AI API keys. Covers three kill-chain stages — SimpleHelp exploitation probes and authentication-bypass attempts, Djinn stealer access to cloud and AI credential stores, and outbound C2 transmissions to known Djinn infrastructure.
- **SonicWall SMA 1000 Pre-Auth Exploitation Detector (Volexity)** (Python) *(draft)* — Scans plain-text SonicWall SMA 1000 appliance access log exports for pre-authentication exploitation indicators surfaced by Volexity across three kill-chain stages — unauthenticated reconnaissance and version-fingerprinting probes targeting SMA 1000 management endpoints, CVE-2025-23006 pre-auth deserialization exploitation attempts via malformed POST requests to appliance management URIs, and post-exploitation staging indicators including webshell access patterns and credential-extraction sequences. Each finding is labeled with a MITRE ATT&CK technique ID to support immediate defender triage.
- **SonicWall SMA1000 CVE-2026-15409 / CVE-2026-15410 Exploit Detector** (Python) *(draft)* — Scans plain-text HTTP access or SSL-VPN log exports for active exploitation of CVE-2026-15409 and CVE-2026-15410 against SonicWall SMA1000 appliances, covering pre-authentication path-traversal probes targeting management and VPN endpoints, authentication-bypass payload injection, and post-exploitation session-abuse indicators. Each match is labeled with a MITRE ATT&CK technique ID, kill-chain stage, severity, and the verbatim log line that triggered it.
- **SonicWall SMA1000 Post-Exploitation Persistence Detector** (Python) *(draft)* — Scans plain-text SonicWall SMA1000 syslog and admin audit log exports for post-exploitation persistence indicators tied to the UTA0533 campaign — covering TinyShell backdoor deployment, malicious HTTPD binary replacement, and appliance-level persistence via cron, rc.local, and SSH key injection. Extends uta0533_sonicwall_preauth_ttp_detector downstream of the initial access phase, tagging each finding with a kill-chain stage and MITRE ATT&CK technique ID to support incident-response triage.
- **SonicWall SMA1000 Zero-Day Exploit Chain & Inc Ransomware Detector** (Python) *(draft)* — Scans plain-text SonicWall SMA appliance access log exports for chained zero-day exploitation signatures across three kill-chain stages — unauthenticated path-traversal and authentication-bypass probes targeting SMA1000 management and SSL-VPN endpoints, OS command injection and credential-harvesting sequences exploiting the CVE-2021-20035/CVE-2023-44221 chained RCE pattern, and Inc ransomware pre-deployment staging and lateral-movement indicators observed in post-SonicWall-compromise telemetry. Each finding is labeled with a MITRE ATT&CK technique ID to support immediate SonicWall defender triage.
- **Spirals Ransomware Kill Chain Detector (Sub-24h Pre-Encryption)** (Python) *(draft)* — Scans plain-text Windows event log text exports or SIEM key-value exports for behavioral indicators of the Spirals ransomware sub-24-hour kill chain across three pre-encryption stages: initial access via credential abuse or phishing-dropped execution, lateral movement through SMB admin-share pivoting and NTLM relay, and pre-encryption staging via shadow copy deletion, AV process termination, and bulk file enumeration. Each match is labeled by kill-chain stage with severity, source identifier, and the verbatim log line that triggered it.
- **Splunk Unauth File-Op Triage** (Python) *(draft)* — Scans plain-text Splunk Enterprise access logs for unauthenticated file-operation attempts matching CVE-2026-20253 exploit patterns, then cross-checks a supplied version string to confirm whether the targeted instance carries the patch. Produces a timestamped stdout report suitable for SOC triage or portfolio evidence.
- **TELEPUZ ClickFix Terminal-Paste & C2 Beacon Cadence Detector** (Python) *(draft)* — Parses plain-text endpoint and proxy log exports for behavioral indicators of TELEPUZ's ClickFix delivery chain, detecting terminal-paste execution patterns (Windows Terminal or cmd.exe spawned with pasted payloads from a browser or lure page) and periodic C2 beacon cadence matching Elastic-documented timing windows. Each finding is labeled with its kill-chain phase and a MITRE ATT&CK technique ID.
- **ToddyCat Umbrij Gmail OAuth Token Abuse Detector** (Python) *(draft)* — Scans Google Workspace audit log exports for ToddyCat Umbrij-style OAuth token abuse patterns, flagging service-account API calls carrying gmail.readonly, mail.google.com, or gmail.modify scopes that fall outside expected delegated-access baselines. Covers three kill-chain stages — OAuth scope grant anomalies where service accounts receive unexpected Gmail scopes, mailbox access where service-account tokens are exercised against individual user mailboxes, and bulk enumeration indicative of ToddyCat's systematic mail collection.
- **VMware ESXi/vCenter Auth Bypass to VM Escape Chain Detector** (Python) *(draft)* — Scans plain-text vCenter vpxd.log, ESXi hostd.log, or shell audit log exports for exploitation and post-exploitation indicators across the CVE-2026-59309 (CVSS 9.8) authentication bypass to VM escape chain. Each finding is labeled with kill-chain stage and MITRE ATT&CK for IaaS technique ID to support SOC triage, purple-team coverage gap analysis, and hands-on defender upskilling against hypervisor-level threats.
- **Veil#Drop TTP Detector** (Python) *(draft)* — Scans plain-text proxy or endpoint log exports for indicators of the Veil#Drop campaign across three kill-chain stages — Blogspot-hosted stage-2 payload staging, PowerShell download cradle execution, and PureLog infostealer memory-resident activity. Each finding is labeled with the corresponding MITRE ATT&CK technique ID to support triage and purple-team coverage gap analysis.
- **VeloCloud Orchestrator Command Injection Zero-Day Detector & Patch Verifier** (Python) *(draft)* — Scans plain-text HTTP access log exports from VMware VeloCloud Orchestrator for active exploitation of the command injection zero-day by matching attacker-observed request patterns targeting management API endpoints where shell metacharacters are injected into query parameters or POST body fields processed by the Orchestrator's server-side execution context. Doubles as a patch-verification checklist by flagging whether patched-endpoint response codes and redirect behaviors consistent with a remediated appliance are absent from the log window, surfacing residual exposure risk alongside live exploitation evidence.
- **Voice Assistant Injection Detector** (Python) *(draft)* — Simulates notification-borne prompt injection payloads to model how malicious content in alerts or messages can hijack AI voice assistant behavior. Analyzes plain-text assistant action logs for heuristic anomalies indicating the assistant may have acted on injected instructions rather than legitimate user commands.
- **WP-SHELLSTORM Backdoor Scanner** (Python) *(draft)* — Scans WordPress PHP source files for backdoor indicators derived from IOCs exposed in the WP-SHELLSTORM operation's own leaked server, covering eval-decode obfuscation chains, HTTP-input-to-sink wiring, and shell-tool fingerprints (FilesMan, WSO, c99) documented in the leak. Emits one labeled alert line per detection with severity, rule name, file path, and line number, enabling incident responders to triage compromised installations without external dependencies.
- **WinRE XML BitLocker Bypass Detector** (Python) *(draft)* — Simulates the GreatXML BitLocker bypass technique by generating synthetic WinRE recovery XML access event sequences, then scans plain-text Windows logs for the same anomalous ReAgent.xml and BCD file access signatures. Outputs flagged log lines and a ready-to-import Sigma or Elastic EQL detection rule to stdout.
- **Woodgnat/Mistic Kill Chain Detector** (Python) *(draft)* — Scans plain-text endpoint log exports for Woodgnat/Mistic campaign indicators: photo-themed ZIP phishing delivering Node.js implants to hospitality-sector front-desk machines. Detects process-spawn anomalies (node.exe from non-developer parents), image-named archive extraction events, and hardcoded Mistic C2 network IOCs across three kill-chain stages.
- **Woodgnat/Mistic Native Messaging Kill Chain Detector** (Python) *(draft)* — Scans plain-text process/event logs for Native Messaging host invocations spawned by browser extension processes, a staging technique observed in the Woodgnat/Mistic ransomware kill chain. Emits timestamped, severity-ranked alerts to stdout when suspicious parent-child process chains or messaging manifest paths match known-bad patterns.
- **XFS CVE-2026-64600 Race-Condition Exploit Detector for RHEL** (Python) *(draft)* — Scans plain-text auditd log exports for behavioral signatures of CVE-2026-64600 XFS race-condition exploitation on default RHEL installs, covering concurrent fallocate and ioctl syscall bursts on XFS-mounted paths from unprivileged UIDs, anomalous EUID-0 transitions following XFS metadata manipulation, and post-exploitation root shell spawning from implicated PID lineages. Each match is labeled by kill-chain stage with severity, syscall name, PID, UID, EUID, and the verbatim log line.
- **gRPC C2 Beacon & HTTP/2 Streaming Anomaly Detector (MODBEACON)** (Python) *(draft)* — Analyzes TLS-inspecting HTTP proxy access logs for gRPC-based command-and-control activity matching MODBEACON behavioral signatures, detecting application/grpc content-type traffic to non-allowlisted endpoints and persistent server-streaming RPC sessions indicative of encrypted C2 beacon keepalive channels. Covers three signal classes across the C2 kill chain: gRPC requests to non-standard or bare-IP destinations, long-lived HTTP/2 streaming connections exceeding configurable duration thresholds, and anomalous gRPC initiator user-agent patterns inconsistent with legitimate enterprise gRPC tooling — each labeled with a MITRE ATT&CK technique ID.
- **macOS Crash-Reporter Identity Masquerade Detector** (Python) *(draft)* — Scans plain-text macOS endpoint logs for processes that fraudulently claim Apple crash-reporter identity—via bundle ID, binary path, or code-signing fields—while concurrently accessing keychain stores or cryptocurrency wallet directories, a pattern consistent with infostealer malware observed in recent macOS supply-chain campaigns. Pairs each finding with the kill-chain stage (Masquerade/KeychainAccess/WalletAccess) and a one-line mitigation to help defenders triage suspicious process activity.
- **snap-confine CVE-2026-8933 Local Privilege Escalation Detector** (Python) *(draft)* — Scans plain-text auditd or AppArmor log exports for behavioral signatures of CVE-2026-8933 snap-confine local privilege escalation exploitation, covering SUID execution anomalies from unexpected parent processes, UID/EUID transition events tied to snap-confine PID chains, and AppArmor profile permit-after-deny sequences indicating confinement bypass. Each match is labeled by kill-chain stage with severity, process name, PID, real and effective UID, and the verbatim log line.
- **wp2shell WordPress Shell Exploitation Detector** (Python) *(draft)* — Scans Apache or Nginx combined-format access log exports for wp2shell exploitation indicators across three kill-chain stages: credential access and authentication abuse targeting wp-login.php and xmlrpc.php, web shell deployment via the WordPress theme/plugin editor and uploads directory, and post-exploitation command execution patterns in wp-content paths. Each finding is labeled with a kill-chain stage, MITRE ATT&CK technique ID, severity, and a one-line mitigation; the final report appends a prioritized rapid-triage checklist tailored to the highest-severity stage observed to support WordPress administrators under active threat.

### Forensics
- **AI Code CWE Pattern Gate** (Python) *(draft)* — Scans source files produced by AI coding agents for CWE-mapped vulnerability patterns using bundled regex rules and blocks CI pipelines by exiting non-zero on policy-violating findings. Covers the hallucination-prone patterns most common in AI-generated code: hardcoded credentials, command injection, SQL injection, unsafe deserialization, weak cryptography, and path traversal.
- **C/C++ Taint Flow Scanner** (Python) *(draft)* — Performs pattern-based taint analysis on C/C++ source files by tracing untrusted input from sources (stdin, argv, fgets) to dangerous sinks (strcpy, system, sprintf). Outputs structured bug reports with file location, taint path, and a reproduction stub to stdout, suitable for embedding in CI pipelines.
- **Fake CVE PoC RAT Scanner** (Python) *(draft)* — Scans a locally cloned GitHub repository for ChocoPoC-style RAT delivery indicators embedded in fake CVE proof-of-concept repos that target security researchers. Detects three attack stages: CVE-impersonation lures in README and metadata, payload staging in GitHub Actions workflows, and RAT installation patterns including base64 decode chains, Discord or Telegram C2 callbacks, and OS persistence commands.
- **HARanalyzer** (Python) — Parses and analyzes HTTP Archive (HAR) files to surface security-relevant requests, headers, and responses.
- **OSS Secret Scanner** (Python) *(draft)* — Scans plain-text source code dumps or CI pipeline log output for leaked credentials using stdlib regex patterns, classifying each finding by blast radius (high/medium/low) based on credential type and revocability. Outputs a triage-ready report to stdout designed for direct integration into CI pass/fail gates.
- **Pwn Request Guard** (Python) *(draft)* — Scans locally cloned repositories for GitHub Actions workflow files that expose agentic pipelines to prompt injection via untrusted issue, PR, or comment body content, flagging direct interpolation of user-controlled context variables alongside over-privileged GITHUB_TOKEN scopes and absent input sanitization guards. Designed to catch the class of vulnerability where AI agent steps receive raw attacker-controlled text capable of hijacking autonomous task execution.
- **Vibe Code Vulnerability Scanner** (Python) *(draft)* — Scans AI-generated application source code for the three dominant vulnerability classes identified in the 434-flaw SecurityWeek analysis of vibe-coded apps: Denial-of-Service patterns, broken authorization, and hardcoded secrets exposure. Designed for CI/CD pipeline gating, it exits non-zero on high-severity findings to block deployment of insecure AI-generated code before it reaches production.
- **list IPs in a pcap** (Python) — Extracts and lists all unique IP addresses observed in a PCAP capture file.
- **list IPs in a pcap with HTTP code NOT 200** (Python) — Extracts IPs from a PCAP that generated non-200 HTTP responses — useful for spotting errors, probes, and attacks.

### IncidentResponse
- **DetectIncommingConnectionandBlockIP** (Python) — Detects suspicious incoming connections and automatically blocks the source IP via firewall rules.

### Labs
- **AI Agent Data Injection Lab** (Python) *(draft)* — Simulates and detects indirect prompt-injection attacks where malicious directives are planted inside HTML comments or product-review text, then consumed by an AI agent whose subsequent unexpected actions (clicks, shell executions) are flagged with the responsible injection phrase and a concrete mitigation. Pairs a realistic attack scenario with detection logic to build hands-on defensive intuition around agent data-injection kill chains.
- **AI Agent Human-in-the-Loop Checkpoint Lab** (Python) *(draft)* — Interactive CLI lab that simulates an AI security agent executing tasks with explicit human approval gates at privilege escalation and data access decision points. Demonstrates how mandatory checkpoints prevent automated exploitation by requiring operator sign-off before sensitive operations proceed.
- **AI IDE Prompt Injection Escape Lab** (Python) *(draft)* — Simulates and detects zero-click prompt injection sandbox escapes in AI-powered IDEs by scanning endpoint process logs for anomalous child-process lineage where editor processes (VS Code, Cursor, Windsurf) spawn unexpected shell interpreters or credential-harvesting commands. Pairs each finding with the responsible injection technique and a concrete mitigation to build hands-on defensive intuition.
- **AI Security Event Triage Pipeline (Self-Hostable Lab)** (Python) *(draft)* — A self-hostable security event triage pipeline modelled after cost-optimized AI-driven SIEM workflows that scores, deduplicates, and priority-ranks plain-text security log events by multi-signal severity heuristics — approximating the signal-extraction logic of open-weight triage models without requiring cloud AI services or external dependencies. Ships with bundled scoring rules covering credential abuse bursts, privilege escalation chains, and lateral movement sequencing to serve as a reproducible lab for learning AI-assisted alert-fatigue reduction patterns.
- **AI-Generated Code CWE Pattern Scanner** (Python) *(draft)* — Scans plain-text source code files for vulnerability patterns that AI code generators most commonly introduce, mapping each finding to its CWE class with a bundled before/after fix example to support developer remediation. Designed to slot into a CI/CD pipeline as a pre-merge gate that fails the build on critical findings and emits a per-CWE tally for tracking AI-introduced vulnerability trends over time.
- **AWS AI-Assisted Attack Chain Lab** (Python) *(draft)* — Simulates a three-stage AI-assisted AWS attack chain — IMDS credential theft, IAM role pivoting, and S3 exfiltration — generating synthetic CloudTrail-style plain-text log events for each stage, then evaluates them against bundled GuardDuty finding-type signatures and CloudTrail API detection rules to show defenders exactly what evidence each attack phase leaves behind. Pairs each simulated event with the specific GuardDuty finding type and CloudTrail eventName that would surface it, giving purple teams a concrete map from attacker action to detection signal.
- **Agent Runtime Enforcement Lab** (Python) *(draft)* — Simulates a stream of AI agent process events mixing benign operations with breach attempts across syscall, namespace-escape, and data-exfiltration scenarios, then evaluates each event against three runtime enforcement strategies (seccomp syscall filtering, Linux namespace isolation, SIGKILL kill-switch) inspired by ButterClaw's breach model. Streams a per-event audit log to stdout and closes with a strategy comparison table showing per-strategy block rates, bypass gaps, and a LAB PASS/FAIL verdict.
- **Agentjacking Prompt Injection Lab** (Python) *(draft)* — Simulates and detects agentjacking attacks where clean-looking GitHub repositories embed hidden prompt-injection payloads targeting AI coding agents during repo setup. Scans a local repository directory for Unicode steganography, AI-targeting instruction files, and encoded payloads in setup scripts, then pairs each finding with a concrete mitigation.
- **AsyncAPI Compromised Package Botnet Loader Chain Lab & Signature Generator** (Python) *(draft)* — Scans npm install logs, audit output, or postinstall hook execution traces for the three-stage @asyncapi supply-chain compromise chain — malicious postinstall injection, secondary loader download, and C2 beacon establishment — producing per-stage alert lines alongside auto-generated YARA rule blocks and npm overrides JSON that defenders can immediately deploy. Designed as a hands-on training lab to walk analysts through the full botnet-loader kill chain using real-world npm log artifacts.
- **CVE-Bench Patch Reasoning Evaluator** (Python) *(draft)* — Parses AI agent session logs from cve-bench runs and scores patch reasoning quality across memory corruption, deserialization, and auth-bypass CVE classes against curated expected-fix indicators. Reports per-CVE pass/fail verdicts, per-class aggregate pass rates, and a final benchmark score to surface systematic blind spots in AI agent patch reasoning.
- **Chrome Extension AI-Impersonation Behavioral Lab** (Python) *(draft)* — Scans an unpacked Chrome extension directory for behavioral indicators of AI-impersonation: address-bar keylogging via omnibox/input event listeners and search-query proxying to non-vendor endpoints. Pairs each finding with the specific permission or code pattern exploited and a concrete mitigation to build defensive intuition.
- **ClickFix CAPTCHA Lure & ACR Stealer Kill Chain Detection Lab** (Python) *(draft)* — Parses plain-text endpoint log exports for behavioral indicators of ClickFix CAPTCHA lure attacks, detecting the three-stage kill chain UAC-0145 uses to trick users into clipboard-pasting malicious commands that deploy ACR Stealer. Each finding is labeled with its kill-chain stage and paired with a concrete mitigation to build analyst intuition for this social-engineering technique.
- **ClickFix → ACR Stealer Kill Chain Detection Lab** (Python) *(draft)* — Parses plain-text Windows process-creation event exports and M365 Unified Audit Log entries for indicators spanning the full ClickFix social-engineering execution chain through ACR Stealer credential theft and OneDrive file exfiltration. Maps each match to its kill-chain stage with a paired mitigation and emits a disruption checklist ordered from earliest to latest viable intervention point.
- **ConsentFix & ClickFix OAuth Token Theft Lab** (Python) *(draft)* — Simulates and detects ConsentFix and ClickFix OAuth token theft campaigns against M365 tenants by scanning plain-text Unified Audit Log exports for anomalous consent grants and stolen-token replay indicators. Pairs each finding with the responsible social-engineering technique and a concrete mitigation to build hands-on defensive intuition.
- **DEBULL M365 Device-Code Phishing Detection Lab** (Python) *(draft)* — Parses plain-text M365 Unified Audit Log exports for indicators of DEBULL-style device-code phishing, detecting attacker activity across the three OAuth device-code flow stages a defender must interrupt before token issuance. Each finding is labeled with its kill-chain stage and paired with a concrete mitigation to build analyst intuition for this campaign class.
- **DHCPv6 CVE-2026-53921 OpenWrt Stack Overflow Lab** (Python) *(draft)* — Builds a labeled byte-level walkthrough of the malformed DHCPv6 SOLICIT packet that triggers the CVE-2026-53921 stack overflow in OpenWrt's DHCPv6 option parser, then emits a ready-to-deploy Suricata detection rule and optionally scans plain-text Suricata fast.log or eve.json exports for matching traffic signatures. Each finding is annotated with the abused protocol field, severity, and a concrete mitigation pointer.
- **DShield SIEM Threat Hunt Lab** (Python) *(draft)* — Parses plain-text DShield honeypot log exports and walks practitioners through three guided threat hunting scenarios — port-sweep reconnaissance, brute-force clustering, and exploit-probe triage — printing step-by-step findings that mirror what analysts build and query inside the DShield ELK 8.19.15 SIEM dashboards. Each lab step pairs a hunting hypothesis, the detection logic applied to the log data, a ranked attacker table, and a copy-pasteable KQL query for the corresponding ELK dashboard panel.
- **EDR Kill Chain Lab** (Python) *(draft)* — Replays a macOS non-admin EDR-disable chain by printing ordered TTP steps with expected telemetry signatures, then scans a plain-text unified log export for the same indicator sequence to validate detection coverage. Designed as a blue-team training tool that teaches both the attack pattern and the log queries that catch it.
- **FastJson RCE Exploitation Lab** (Python) *(draft)* — Generates a synthetic HTTP access log seeded with FastJson JNDI-injection payloads — baseline @type gadget chains, JdbcRowSetImpl callbacks, and unicode/URL-encoded @-sign bypass variants — then scans any plain-text log for the same patterns, emitting a paired WAF rule stub and SIEM detection note alongside each finding. Covers the CVE-2017-18349 and CVE-2022-25845 FastJson @type deserialization family through concrete attack-and-defense examples.
- **Forg365 AiTM + Device-Code Phishing Detection Lab** (Python) *(draft)* — Parses plain-text M365 Unified Audit Log exports for indicators of Forg365-style adversary-in-the-middle proxy phishing and OAuth device-code phishing, mapping each hit to its kill-chain stage across both attack paths with a paired KQL rule snippet and concrete mitigation. Outputs a prioritized kill-chain disruption checklist covering the critical intervention windows where a defender can sever the attack before session cookies or device-code tokens reach the threat actor.
- **GhostApproval Symlink Attack Lab** (Python) *(draft)* — Demonstrates the GhostApproval symlink attack by planting a crafted symlink inside a disposable sandbox that silently redirects an AI coding agent's write to a caller-supplied file outside the permitted directory, then replays the identical write through a hardened path-canonicalization guard that detects and blocks the escape. Pairs each phase with a printed explanation of the attack vector and the exact defensive fix applied.
- **GitLab Authenticated RCE via Malicious Git Push Lab** (Python) *(draft)* — Builds a disposable bare-repo sandbox demonstrating how a crafted git push can trigger server-side hook execution in GitLab self-managed instances, then scans plain-text GitLab production or Gitaly log exports for anomalous git subprocess spawning patterns that indicate active exploitation. Pairs each finding with the specific hook-abuse technique and a concrete mitigation.
- **GitLost Issue Harvesting Lab** (Python) *(draft)* — Simulates and detects the GitLost attack pattern where unauthenticated attackers craft malicious GitHub Issues to harvest private repository data via misconfigured Actions workflows in public org repos. Scans a local repository clone for workflow configurations that expose org secrets or cross-repo tokens to issue-body-controlled execution paths, pairing each finding with a concrete hardening note.
- **HollowByte OpenSSL DoS IDS Rule Lab & Validator** (Python) *(draft)* — Scans plain-text TLS session logs and Suricata fast.log or Zeek conn.log exports for the HollowByte OpenSSL denial-of-service trigger pattern — an anomalous 11-byte ClientHello extension payload that crashes unpatched OpenSSL during handshake processing — and pairs live log detection with a controlled lab mode that synthesizes trigger entries across three validation scenarios to confirm rule fidelity before production deployment. Each finding is labeled with a MITRE ATT&CK technique ID and cross-referenced against generated Suricata and Zeek rule stubs to support immediate IDS tuning and defender triage.
- **LLM Security Benchmark** (Python) *(draft)* — Scores LLM-generated responses against a fixed rubric of security research tasks including CVE triage, exploit summarization, and malware prompt analysis. Outputs a ranked scorecard to stdout so practitioners can compare models on domain-specific accuracy.
- **LLM vs Hybrid Vulnerability Scanner Benchmark** (Python) *(draft)* — Scans a corpus of plain-text source files with two pipelines — a broad LLM-style heuristic scanner and a hybrid pipeline that layers separation-logic-inspired context filters on top — to benchmark false-positive rates between the two approaches. Computes and reports per-pipeline precision and false-positive rate against an optional ground-truth file, giving security researchers a reproducible baseline for comparing pure-LLM and logic-constrained vulnerability detection strategies.
- **LUKS Dead Man's Switch Lab** (Python) *(draft)* — Simulates and audits LUKS dead man's switch configurations by replaying plain-text system log events to detect USB-absence trigger conditions and timer-based lockout windows, pairing each finding with a concrete hardening recommendation. Demonstrates how endpoint full-disk encryption can be configured to auto-lock when a trusted USB token is removed or a heartbeat timer expires, teaching the gap between intended and actual trigger coverage.
- **MCP Security Proxy Injection Lab** (Python) *(draft)* — Simulates a stream of MCP tool-call and tool-response messages mixing benign traffic with prompt injection payloads across three attack scenarios (direct, encoded, evasion), then runs each message through a configurable security proxy that audits and blocks based on policy rules. Streams a per-message allow/block audit log to stdout and closes with a lab summary showing detection rate, evasion gaps, and per-scenario block counts.
- **Multi-Brand Fake Job Interview Phishing & OAuth Lure Lab** (Python) *(draft)* — Parses plain-text email header dumps for indicators of multi-brand fake job interview lure campaigns, detecting impersonation of LinkedIn, Indeed, Glassdoor, and similar hiring platforms alongside header-spoofing anomalies and OAuth redirect lures embedded in body fragments. Each finding is labeled with a kill-chain stage and paired with a one-line mitigation to build hands-on analyst intuition for this campaign class.
- **Poisoned Repo Auto-Execution Lab** (Python) *(draft)* — Plants a disposable poisoned-repository skeleton that embeds auto-execution triggers targeting AI coding IDEs — a devcontainer lifecycle hook, a VS Code folder-open task, and an npm postinstall payload — then scans any local repository path for the same patterns using a bundled detection ruleset. Each finding is paired with the specific IDE auto-run surface being abused and a one-line mitigation.
- **Security Log Alert Pipeline Lab** (Python) *(draft)* — A reproducible security alerting pipeline that applies bundled detection rules — modelled after Clickdetect-style structured query logic — to plain-text log files, emitting structured alerts with rule name, severity, and matched context. Ships with sample detection rules covering credential stuffing, lateral movement, and privilege escalation to serve as a hands-on lab for building and testing SIEM-style detection logic without any external dependencies.
- **Spring Boot Actuator Exposure & Heap Dump Credential Lab** (Python) *(draft)* — Scans web server access logs for Spring Boot actuator endpoint reconnaissance (targeting /actuator/heapdump, /actuator/env, and adjacent management paths) and analyzes strings-extracted heap dump text for embedded credentials and secrets, demonstrating the full attacker chain from endpoint discovery to secret extraction. Operates in auto-detect mode by examining input file structure and switching between log-analysis and heap-dump-forensics workflows accordingly.
- **Supabase RLS Cross-Tenant Bypass Lab** (Python) *(draft)* — Parses a Supabase SQL schema file and detects cross-tenant Row Level Security bypass scenarios by auditing table-level RLS enablement, policy operation coverage, and tenant-filter presence in USING and WITH CHECK expressions, then renders an rlsgrid matrix showing per-table protection status across all SQL operations. Pairs each finding with a concrete remediation pattern drawn from multi-tenant schema design best practices to build hands-on defensive intuition.
- **Text-Salted Phishing Evasion & AI Filter Benchmark Lab** (Python) *(draft)* — Generates zero-width-character, homoglyph, and whitespace-padded variants of plain-text phishing email templates, then benchmarks a set of simulated AI email security filter rules against clean and salted samples to measure per-technique evasion rates. Closes with a detection rule block for each salting technique so analysts understand both the attack and the countermeasure.
- **npm Supply Chain Attack Lab** (Python) *(draft)* — Parses a plain-text npm verbose install log to replay a classic postinstall-script supply chain attack, then re-evaluates each detected script under npm 12's opt-in execution model to show exactly what gets blocked. Outputs a side-by-side comparison table and a ready-to-paste allowlist for packages whose postinstall scripts are legitimate.
- **npm Supply Chain Integrity Lab** (Python) *(draft)* — Simulates the DPRK Mastra npm attack pattern by injecting synthetic malicious transitive dependencies into a package-lock.json manifest, then applies regex IOC rules to detect crypto wallet extension targeting via postinstall hooks and typosquatted package names. Outputs a plain-text findings table with severity ratings and MITRE ATT&CK T1195.001 tagging to stdout.
- **wp2shell Exploit Chain WAF Validator** (Python) *(draft)* — Simulates the chained wp2shell unauthenticated RCE kill-chain — credential stuffing, theme-editor shell deployment, and post-exploitation command polling — as an ordered sequence of synthetic HTTP vectors with optional encoding-based evasion variants, then cross-matches each step against configurable ModSecurity-format WAF rule patterns to report per-stage detection coverage. Designed as a pre-deployment lab for security teams to identify detection blind spots in standard WAF rule sets and receive concrete WordPress-specific hardening recommendations before production exposure.
- **wp2shell WAF Detection Lab** (Python) *(draft)* — Generates ModSecurity-compatible WAF rules targeting wp2shell unauthenticated RCE exploitation signatures across credential-stuffing, shell-deployment, and post-exploitation kill-chain stages, then self-validates rule coverage by cross-matching bundled synthetic attack vectors against each generated rule. Designed as a hands-on lab for WordPress security teams to measure rule completeness and identify coverage gaps before production WAF deployment.

### Posture
- **AI Code CWE Pre-Commit Gate with Triage Dashboard** (Python) *(draft)* — Runs as a git pre-commit hook that scans only staged source files for AI-generated code vulnerability patterns mapped to CWE classes, blocking commits on critical findings and appending each scan's structured results to a local JSONL triage log. A built-in dashboard mode reads the cumulative log and renders per-CWE hit-rate rankings, top repeat-offender files, and a 7-day rolling trend to stdout, turning individual commit gates into a measurable shift-left posture metric.
- **AI IDE Path-Hijacking Binary Guard** (Python) *(draft)* — Scans a project directory for unexpected executable files — compiled binaries detected by ELF/PE/Mach-O magic bytes, or files with execute bits set outside an allowlist — that could be used in path-hijacking attacks against AI IDEs such as Cursor, Copilot, or Windsurf. Designed to run as a pre-commit hook or CI step, it exits non-zero on any unexpected executable and surfaces the file path, binary type, and Unix permissions to block the commit or pipeline run.
- **CI/CD Permission & Token Exposure Scanner** (Python) *(draft)* — Scans plain-text CI/CD configuration files and pipeline logs for overly permissive scope declarations and exposed credential patterns that enable unauthenticated repository hijacking. Produces a prioritized risk report to stdout with line-level references and remediation guidance.
- **DownloadDNSRecordsInCSVforADomain** (Python) — Exports all DNS records for a given domain to a CSV file for inventory and monitoring.
- **EDR Telemetry Gap Detector** (Python) *(draft)* — Parses plain-text EDR/syslog output to identify coverage gaps exploitable by AI-assisted evasion techniques and lateral movement via AD discovery. Flags missing telemetry signals that would leave container escapes and privilege escalation undetected.
- **GitHub Verified Badge Spoof Auditor** (Python) *(draft)* — Parses a git log export to detect commit signing anomalies that could allow GitHub's 'Verified' badge to be spoofed on merged PRs: duplicate commit SHAs, bad or expired GPG signatures that GitHub may still badge as verified, and signing key reuse across valid and invalid signatures. Helps repository maintainers confirm that every Verified badge in merged PR history maps to a cryptographically sound, non-colliding signature.
- **Healthcheck** (PowerShell) — System health check script covering key security-relevant services and configurations.
- **Healthcheck (Python)** (Python) — Python version of the system health check for Linux/cross-platform environments.
- **MCP Config Scanner** (Python) *(draft)* — Scans MCP server configuration files for known misconfigurations and supply-chain risks using customizable detection rules. Outputs structured findings to stdout with severity ratings and remediation hints.
- **Shadow AI Agent Discovery and Governance Scanner** (Python) *(draft)* — Scans MCP configuration files and OAuth grant exports for indicators of ungoverned AI agent deployment, flagging unregistered MCP server definitions, embedded AI provider API tokens, and OAuth scope grants authorizing AI service access outside approved workflows. Covers three signal classes across the shadow-AI governance gap: MCPServerPresence for unregistered AI-backend MCP servers, APITokenExposure for commercial LLM API keys embedded in config or environment fields, and OAuthScopeAnomaly for AI-service scope grants issued to non-allowlisted client IDs.
- **findIdP** (Python) — Identifies the Identity Provider (IdP) for a given domain or email address.

### SupplyChain
- **Browser Extension Supply Chain Scanner** (Python) *(draft)* — Statically analyzes installed browser extension source files to detect dormant or obfuscated script-injection capabilities consistent with supply chain compromise. Uses the Adblock for YouTube attack as the reference threat model, flagging eval chains, remote code fetching, obfuscation patterns, and over-privileged manifest permissions.
- **Contagious Interview NPM Payload Scanner** (Python) *(draft)* — Detects behavioral signatures of Contagious Interview threat actor TTPs in npm package audit logs and repository metadata, including malicious install hooks, staged payload delivery patterns, and trojanized developer-tool indicators. Outputs prioritized alerts to stdout for integration into CI/CD pipeline log monitoring workflows.
- **Fake AI Tool & MCP Server Package Detector** (Python) *(draft)* — Scans pip requirements files, npm package manifests, or plain-text install logs for packages fraudulently impersonating AI tools or MCP servers, applying name-pattern heuristics and version-freshness signals derived from the FakeGit SmartLoader campaign. Surfaces imposters of high-value AI libraries such as OpenAI, Anthropic, LangChain, and MCP server frameworks by combining bigram similarity scoring against a curated legitimate-AI-library baseline with newly-published version markers characteristic of freshly-registered fakes.
- **GitHub Actions Packagist Abuse Detector** (Python) *(draft)* — Scans a locally cloned repository's GitHub Actions workflow files and Composer manifests for indicators of supply-chain compromise where malicious Packagist dev-version packages are injected into CI pipelines to harvest cPanel/WHM hosting credentials. Detects three attack stages: dev-version constraint injection in composer.json or workflow run steps, cPanel/WHM credential harvesting in workflow env blocks, and runner exfiltration via outbound curl or webhook relay within the same job.
- **Go Dead Drop Beacon & PowerShell Payload Detector** (Python) *(draft)* — Scans Go module manifests and exported Go source text for indicators of the GitHub-hosted dead drop beaconing pattern documented across 200+ malicious repositories — covering suspicious module import paths, raw-content fetch strings targeting non-Go GitHub paths, and embedded PowerShell payload resolution. Each finding is labeled with a MITRE ATT&CK technique ID to support supply chain triage and dependency audit workflows.
- **HalluSquat Package Registry Threat Detector** (Python) *(draft)* — Reads a plain-text list of AI-recommended package names, queries PyPI or npm registry APIs for each entry via stdlib urllib, and flags packages that are absent (hallucinated name), newly registered within a configurable age window, or show near-zero release activity — the compound risk profile of HalluSquatting, where threat actors pre-register names that AI models are statistically likely to fabricate. Designed as a lightweight pre-install gate to run before executing any AI-suggested dependency installation command.
- **Marketplace Plugin Risk Scanner** (Python) *(draft)* — Scans a plain-text log of IDE plugin metadata (publisher age, download counts, timestamps) to flag suspicious packages using velocity anomalies and publisher freshness heuristics. Outputs risk-scored findings to stdout for use in CI/CD pre-install gates.
- **Marketplace Skill Risk Scanner** (Python) *(draft)* — Statically analyzes AI agent skill and plugin source files for behavioral indicators of malicious activity that evade automated marketplace security scans. Scores each file against a weighted rubric of AIR-documented patterns including prompt injection hooks, covert exfiltration calls, and permission escalation attempts.
- **Miasma/Hades Supply Chain Sentinel** (Python) *(draft)* — Scans plain-text npm package metadata and Go module logs for Miasma/Hades malware-family indicators using heuristic risk scoring of package fields, dependency patterns, and GitHub Actions workflow anomalies. Emits scored, line-per-finding results to stdout so findings can be piped into any alerting pipeline.
- **NPM IOC Log Scanner** (Python) *(draft)* — Scans plain-text CI/CD and npm audit logs for TeamPCP supply chain campaign indicators using embedded YARA-style pattern rules. Detects malicious package names, suspicious install hooks, and known IOC strings associated with npm-based supply chain attacks.
- **NPM Malicious Pattern Scanner** (Python) *(draft)* — Scans npm install or audit log output for behavioral patterns associated with worm-style credential stealers, as documented in the Red Hat supply-chain campaign. Flags suspect package events to stdout so CI/CD pipelines can gate on exit code.
- **NPM Sleet Scanner** (Python) *(draft)* — Scans plain-text npm install logs for Sapphire Sleet supply chain compromise indicators including typosquatted Mastra AI package names, post-install script network telemetry callbacks, and base64-encoded payload execution patterns. Extends the Contagious Interview NPM Payload Scanner ruleset with DPRK-attributed TTPs observed in the 2024–2025 Mastra AI campaign.
- **NuGet Typosquat Supply Chain Monitor** (Python) *(draft)* — Scores each package name in a NuGet dependency list against a bundled corpus of popular libraries using normalized edit-distance similarity, flagging candidates whose names closely resemble a known package but differ by one or two characters — the hallmark of typosquatting campaigns such as Newtonsoftt.Json impersonating Newtonsoft.Json. Surfaces findings with similarity scores and severity bands to support rapid triage before a compromised package reaches the build pipeline.
- **Package Checksum and Maintainer-Key Baseline Gate** (Python) *(draft)* — Ingests a current npm/PyPI package integrity manifest — containing per-package SHA digests and maintainer GPG key fingerprints — and compares it line-by-line against a stored known-good baseline JSON to surface checksum drift and unexpected key rotations before any package code is installed. Emits severity-labeled findings for each deviation with the differing values side-by-side and exits non-zero on high-severity drift to serve as a hard CI/CD pipeline gate.
- **Payment SDK Registry Threat Scanner** (Python) *(draft)* — Scans npm package-lock.json, PyPI requirements files, or plain-text CI install logs for packages that closely resemble trusted payment-processing SDKs using bigram-similarity scoring against a curated baseline of high-value targets such as Stripe, Braintree, Square, and PayPal libraries. Surfaces typosquatted names alongside newly-published version heuristics as a CI/CD gate that blocks suspicious payment-SDK imposters before they enter a production build.
- **PolinRider Dependency Backdoor Scanner** (Python) *(draft)* — Parses a project dependency manifest (requirements.txt, package.json, go.mod, or composer.json) and cross-references each pinned package and version against a plain-text PolinRider compromised-package threat database, flagging backdoored entries with severity labels and safe-version remediation guidance. Designed as a pre-install gate to catch known-malicious supply-chain packages before they reach the build environment.
- **PolinRider Package Registry Threat Detector** (Python) *(draft)* — Scans a locally downloaded package directory for PolinRider-campaign TTPs across npm, Go, and Packagist registries, flagging typosquatting and dependency-confusion naming patterns, metadata anomalies such as version-number abuse and missing maintainer fields, and behavioral indicators in install scripts including base64 decode chains and environment-variable exfiltration. Emits one labeled alert line per detection, enabling supply-chain analysts to triage suspicious packages before installation or CI ingestion.
- **PyPI Trojan Scanner** (Python) *(draft)* — Downloads a PyPI package wheel and scans its extracted Python source files for credential-harvesting stub patterns characteristic of supply-chain trojanization attacks. Reports suspicious code fragments, matched pattern names, and line numbers to stdout without executing any package code.
- **SRI Drift Monitor** (Python) *(draft)* — Parses plain-text web server or CDN access logs to detect when third-party script URLs return content whose hash no longer matches an expected SRI baseline, flagging the drift pattern seen in supply-chain injection attacks like the Polymarket incident. Alerts are written to stdout with timestamp, URL, expected hash, and observed hash for immediate triage.
- **VS Code Task Lifecycle Infostealer Detector** (Python) *(draft)* — Scans plain-text endpoint logs for the supply-chain campaign that plants .vscode/tasks.json inside hijacked npm/Go packages to bypass postinstall-script restrictions and drop a Python infostealer. Detects three kill-chain stages: npm spawning VS Code during package install, VS Code tasks invoking python against credential stores, and outbound connections to campaign-linked exfiltration domains.
- **WordPress Plugin Integrity Scanner** (Python) *(draft)* — Computes SHA256 hashes of installed WordPress plugin files and diffs them against the official WordPress.org SVN repository to detect unauthorized modifications. Surfaces injected backdoor code from supply chain tampering by flagging files that have been added, removed, or altered since the official release.
- **npm Beta/RC Hash and DEV#POPPER RAT Indicator Scanner** (Python) *(draft)* — Reads a package.json to flag beta and release-candidate dependency versions whose SHA256 digests appear in a bundled known-malicious hash list, and applies a Sigma-style rule set to detect DEV#POPPER RAT campaign indicators — including eval/Buffer base64 chains, remote payload fetches, and environment-variable exfiltration patterns — embedded in preinstall and postinstall lifecycle scripts. Emits severity-labeled findings with the matched rule name and script fragment to stdout, and exits non-zero on any high-severity hit to serve as a hard CI/CD pre-install gate.
- **npm Blockchain DNS & On-Chain C2 Resolver Detector** (Python) *(draft)* — Scans npm install logs and audit output for indicators that installed packages resolve command-and-control addresses through blockchain DNS systems (ENS, Unstoppable Domains, Handshake) or direct Web3 JSON-RPC on-chain lookups, a censorship-resistant C2 technique observed in recent supply-chain campaigns. Surfaces matches with kill-chain stage labels (C2Resolution, OnChainLookup, BeaconDelivery) and severity scores to support pre-merge triage of dependency changes before malicious infrastructure addresses reach production.
- **npm Install-Script Native Binary Drop Scanner** (Python) *(draft)* — Scans a package.json or package-lock.json for preinstall and postinstall lifecycle scripts that download, compile, or execute native binaries — including Rust-toolchain artifacts — using a bundled set of Sigma-style detection rules to surface supply-chain implant staging before npm install runs. Emits one labeled alert per match with severity, rule name, and matched script fragment to support CI/CD gate integration and pre-merge security review.
- **npm RAT Behavior & Exfiltration Monitor** (Python) *(draft)* — Scans npm postinstall hook execution traces or install logs for two behavioral signatures characteristic of supply-chain remote-access trojans: credential and API-key exfiltration via environment variable harvesting and sensitive file reads, and rogue outbound network callbacks to non-registry or known C2 endpoints embedded in lifecycle-script output. Surfaces findings with kill-chain stage labels and severity scores to support rapid triage of newly published packages before secrets or shell access leave the build environment.
- **npm SDK Exfiltration & Rogue Network Call Monitor** (Python) *(draft)* — Scans npm install logs, audit output, or postinstall hook execution traces for malicious SDK package behavior across two kill-chain stages: credential and API key exfiltration via environment variable harvesting and sensitive file access, and unexpected outbound network calls to non-registry or known C2 endpoints surfaced in lifecycle-script execution output. Surfaces findings with severity labels to support rapid triage of compromised SDK packages before secrets leave the build environment.
- **npm Typosquat & Impersonator Detection Lab** (Python) *(draft)* — Scans a package.json or plain-text npm install log for typosquatted and impersonator packages by computing bigram-overlap name-similarity scores against a curated popular-package baseline, then inspects lifecycle scripts for outbound network callbacks and credential-harvesting patterns. Designed to run as a CI/CD gate that surfaces suspicious dependency additions before they reach production.
- **npm/PyPI Package Compromise Detector** (Python) *(draft)* — Scans plain-text npm/PyPI registry event logs or CI pipeline output for three supply-chain compromise signals: checksum drift between published and expected package integrity hashes, unexpected maintainer account additions, and outbound connections to known cryptocurrency wallet-exfiltration endpoints surfaced in install-hook execution logs. Surfaces findings with severity labels across three detection stages to support rapid triage of compromised package events before they reach production.

### ThreatHunting
- **CVE Triage Scanner** (Python) *(draft)* — Parses plain-text CVE feed logs to score exploitability based on keyword heuristics and CVSS indicators, then drafts Sigma detection rules for high-severity findings. Outputs triage results and detection rules to stdout without requiring external dependencies or human prompting.
- **Fake Security Tool Repository Detector** (Python) *(draft)* — Queries the GitHub Search API for repositories impersonating a named legitimate security tool and scores each candidate against bundled YARA-style regex rules targeting typosquatted names, infostealer distribution keywords, and social-engineering lures. Emits one scored alert per suspicious repository to stdout to support analyst triage of fake-tool campaigns distributing infostealers at scale.
- **FakeGit Repository Threat Hunter** (Python) *(draft)* — Scans plain-text CI/CD logs, bash history, git configs, or pipeline output for GitHub repository references and scores each unique slug against a multi-factor heuristic ruleset derived from the FakeGit SmartLoader/StealC campaign — covering AI/MCP keyword impersonation, typosquat similarity against a legitimate-repo baseline, and raw-content payload delivery patterns characteristic of the campaign. Surfaces suspicious repos before SmartLoader or StealC payloads execute, with MITRE ATT&CK labels and a severity-gated exit code for use as a CI/CD pre-execution gate.
- **FortiSandbox Post-Exploitation Scanner** (Python) *(draft)* — Applies embedded Sigma-style detection rules to plain text FortiSandbox/FortiGate logs to surface post-exploitation indicators consistent with the 30,000-credential compromise baseline. Outputs severity-ranked matches to stdout for triage without requiring external tooling or SIEM integration.
- **Ivanti Sentry Sigma Rule Matcher** (Python) *(draft)* — Scans plain-text log files for attacker pre-positioning patterns associated with post-disclosure exploitation of Ivanti Sentry edge appliances. Matches log lines against a built-in Sigma-style rule set covering CVE-2023-38035 and related recon/exploitation indicators, printing structured runbook alerts to stdout.
- **OWAReaper Exchange OWA Module Drop & Void Blizzard TTP Detector** (Python) *(draft)* — Scans plain-text IIS W3C access log exports from Microsoft Exchange servers for OWAReaper intrusion-set activity across three kill-chain stages: malicious OWA HttpModule and IIS module-drop indicators, command-and-control beacon patterns tunneled through compromised OWA endpoints, and Void Blizzard credential-harvesting and lateral-movement TTPs targeting Exchange Control Panel and Exchange Web Services. Each match is labeled with a MITRE ATT&CK technique ID, kill-chain stage, severity, and the verbatim log line that triggered it.
- **PeopleSoft CVE-2026-35273 Hunt Rule Generator** (Python) *(draft)* — Scans plain-text PeopleSoft application logs for CVE-2026-35273 exploitation indicators attributed to ShinyHunters TTPs and emits Sigma-compatible detection rules to stdout. Extends the WebLogic CVE-2024-21182 hunt rule pattern with PeopleSoft-specific URI traversal, credential-stuffing, and bulk-export signatures.
- **RedCAP Long-Dwell Credential Theft Detector** (Python) *(draft)* — Analyzes plain-text authentication logs for behavioral patterns consistent with UNC6508-style credential theft campaigns targeting research institutions, including low-and-slow login enumeration, off-hours access spikes, and credential reuse across research portals. Outputs a prioritized alert report to stdout with dwell-time estimates and MITRE ATT&CK mappings.
- **Tailscale+SSH Persistence Detector** (Python) *(draft)* — Parses plain-text system logs to detect co-installation of Tailscale and OpenSSH on endpoints, flagging the combination as a potential covert persistence indicator. Outputs structured Sigma-style alert findings to stdout for triage or pipeline ingestion.
- **UTA0533 SonicWall Pre-Patch Exploitation & Custom Malware TTP Detector** (Python) *(draft)* — Scans plain-text SonicWall SMA appliance access log exports for UTA0533 intrusion-set activity across three kill-chain stages: pre-authentication reconnaissance and exploitation probes targeting unpatched SMA management and SSL-VPN endpoints, custom malware staging and implant-delivery URI patterns attributed to UTA0533's post-access toolkit, and credential-harvesting plus VPN session-hijack indicators consistent with the group's dwell-time persistence tradecraft. Each match is labeled with a MITRE ATT&CK technique ID, kill-chain stage, severity, and the verbatim log line that triggered it.
- **VS Code URI Token Hunter** (Python) *(draft)* — Scans plain-text CI/CD pipeline logs for indicators of GitHub token exfiltration via VS Code workspace trust abuse, flagging suspicious URI handler invocations, malicious .vscode/settings.json patterns, and credential exposure sequences. Outputs timestamped findings with severity and matched rule to stdout for rapid triage by pipeline defenders.
- **WP2Shell CVE-2026-60137 / CVE-2026-63030 WAF Detection & Threat-Hunt Scanner** (Python) *(draft)* — Scans Apache or Nginx combined-format access log exports for exploitation signatures targeting CVE-2026-60137 (WordPress REST API unauthenticated file-write) and CVE-2026-63030 (wp-cron authentication-bypass shell staging) across three kill-chain stages: initial exploitation probe, web-shell deployment, and post-compromise dwell-time beaconing. A companion hunt mode identifies already-compromised WordPress instances by surfacing dormant shell polling patterns, abnormal wp-cron activation cadence, and staged payload paths that evade reactive WAF rules.
- **WebLogic CVE-2024-21182 Hunt Rule Generator** (Python) *(draft)* — Parses plain-text HTTP/WebLogic access and server logs to detect exploitation patterns matching CVE-2024-21182 (JNDI/T3 deserialization abuse) as tracked in active KEV campaigns. Outputs structured Sigma-style detection hits with timestamp, source IP, and matched pattern to stdout.
- **Webshell IOC Forge** (Python) *(draft)* — Scans plain-text web server access logs for obfuscation patterns and post-drop behaviors associated with modern webshell variants catalogued in SANS ISC diaries. Emits ranked YARA string/condition stubs and Sigma detection/condition YAML stubs for the highest-confidence indicator clusters found.
- **Wireless ADB C2 & Lateral Movement Detector** (Python) *(draft)* — Scans plain-text network connection logs for TCP/5555 (Wireless ADB) sessions that lack evidence of prior USB pairing, flagging unauthorized remote shell access, lateral movement across Android devices, and outbound C2 callbacks to non-RFC1918 addresses. Each finding is labeled with a MITRE ATT&CK technique ID, kill-chain stage, and a one-line mitigation to support mobile threat-hunting and incident triage.
- **Xeno RAT Log Hunter** (Python) *(draft)* — Scans plain-text log files for Xeno RAT indicators including C2 registration patterns, persistence registry keys, and network beaconing signatures. Outputs timestamped SIGMA-aligned findings to stdout, tuned for government finance sector environments.

## RedTeam

### Labs
- **AWS Privilege Escalation Probe** (Python) *(draft)* — Simulates IAM privilege-escalation and S3 misconfiguration scenarios against a LocalStack endpoint so practitioners can learn and test detection logic offline. Outputs a structured audit trail to stdout showing each attack path attempted and whether it succeeded.
- **PhishBench** (Python) *(draft)* — Fires configurable prompt-injection phishing payloads at AI email agent endpoints and measures resilience by parsing stdout responses for compliance or refusal signals. Benchmarks multiple model/config profiles in a single run and reports a scored summary to stdout.
- **Prompt Injection Lab** (Python) *(draft)* — Demonstrates prompt injection and data exfiltration attacks against a simulated RAG pipeline, then shows how zero-trust input/output validation mitigations block each attack vector. Runs attack scenarios end-to-end in a single script with annotated stdout output for hands-on learning.
- **Vulnerability Triage Lab** (Python) *(draft)* — Scans a deliberately vulnerable Python codebase for common vulnerability patterns (SQLi, command injection, hardcoded secrets, path traversal) and produces a prioritized triage report. Guides practitioners through reading findings and understanding why each pattern is dangerous.

- **PingScan** (Shell) — ICMP ping sweep to discover live hosts on a network range.
- **SearchKeyWordsInAWebsite** (Python) — Crawls a website and searches all pages for specified keywords — useful for recon and content discovery.
- **SearchKeyWordsInAWebsiteThroughTor** (Python) — Same as SearchKeyWordsInAWebsite but routes traffic through the Tor network for anonymity.
- **TelnetPortScanner** (Shell) — Port scanner using telnet to test connectivity to common service ports.
- **TestFiletypeuploads** (Python) — Tests file upload endpoints for weak extension/MIME type restrictions that could allow malicious uploads.

## GRC

- **Agent Policy Auditor** (Python) *(draft)* — Parses plain-text agent audit logs to detect over-privileged access patterns when autonomous agents interact with Postgres and Kubernetes. Flags violations of least-privilege policy rules and reports findings to stdout.
- **CVE Asset Correlator** (Python) *(draft)* — Ingests a CVE list and a plain-text SBOM/CMDB asset inventory, correlates vulnerabilities against affected assets, and outputs prioritized remediation tickets to stdout. Ranks findings by CVSS severity and asset criticality to surface the highest-risk exposures first.
- **GRC Evidence Gap Scanner** (Python) *(draft)* — Parses plain-text audit logs against a JSON-defined control catalog and surfaces controls whose evidence is missing or outside the required collection window. Outputs a prioritized remediation list to stdout, simulating the ticket-opening step a full agent would automate.
- **UploadExceltoSharepointList** (PowerShell) — Uploads data from an Excel file into a SharePoint list for compliance tracking and reporting.
- **UploadExceltoSharepointListUsingPrimaryKey** (PowerShell) — Uploads Excel data to SharePoint using a primary key to prevent duplicate entries.
- **checkHostingPlatform** (Python) — Identifies the hosting platform (AWS, Azure, GCP, etc.) for a given domain or IP.
- **dnsinventory** (Python) — Enumerates and inventories DNS records across multiple domains for asset management and compliance.
