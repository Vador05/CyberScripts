# CyberScripts — Tool Index

*Last updated: 2026-07-06 — 107 tools*

## BlueTeam

### Deception
- **Create a Deceptive User in AD** (PowerShell) — Creates a honeypot user account in Active Directory to detect lateral movement and credential attacks.

### Detection
- **AI API Cross-Tenant Leak Detector** (Python) *(draft)* — Scans plain-text AI platform API access logs for cross-tenant data leakage patterns modeled on known Dify vulnerabilities, including unauthorized document preview bypass and unauthenticated internal API exposure. Emits NDJSON alerts to stdout when tenant boundary violations are detected across dataset, document, and console API endpoints.
- **AI Agent Attack Chain Behavioral Detector** (Python) *(draft)* — Scans plain-text API gateway, LLM orchestration, or cloud function execution logs for behavioral signatures of autonomous AI agent attack chains across three kill-chain stages — reconnaissance via automated API enumeration and metadata harvesting, lateral movement through agent-mediated credential relay and cross-service tool pivoting, and impact via LLM-orchestrated bulk file access or encryption-stage KMS abuse. Distinguishes agentic attack sessions from human-operated or scripted traffic by analyzing inter-request timing regularity, burst-pattern cadence, and sequential tool-call ordering that are statistically inconsistent with human interaction.
- **AI Coding Assistant Prompt Injection Detector** (Python) *(draft)* — Scans plain-text IDE and AI coding assistant activity logs for prompt-injection signatures that mirror the Amazon Q CVE attack pattern, flagging attempts to coerce the assistant into accessing and exfiltrating cloud credentials via malicious repository content. Correlates injection trigger phrases with downstream credential-access and outbound-transfer indicators within a sliding line window to surface high-confidence exfiltration chains.
- **AI Pipeline Prompt Injection Detector** (Python) *(draft)* — Scans plain-text logs from AI-assisted malware analysis pipelines for adversarial natural language payloads that attempt to hijack, abort, or corrupt the analysis by targeting the underlying LLM's instruction-following behavior. Emits flagged lines with severity scores, matched signatures, and contextual snippets to stdout so analysts can identify evasion-aware malware before it poisons automated verdicts.
- **AI Recon Pattern Detector** (Python) *(draft)* — Analyzes plain text auth/syslog entries to identify behavioral signatures of LLM-driven lateral movement, such as systematic sequential probing and structured host enumeration chains. Outputs scored alert lines to stdout for use in developing detection rules against autonomous AI-reasoned reconnaissance.
- **AiTM PhaaS Token Harvest & M365 OAuth Abuse Detector** (Python) *(draft)* — Scans plain-text M365 or Azure AD sign-in log exports for adversary-in-the-middle token harvesting patterns associated with ARToken/EvilTokens PhaaS infrastructure across three kill-chain stages — AiTM staging where reverse-proxy indicators and known PhaaS redirect URIs appear in OAuth flows, token harvesting where authorization codes or session tokens are intercepted mid-flow, and replay abuse where captured tokens are reused from mismatched IP addresses or user-agents indicating post-harvest account takeover. Targets the PhaaS ecosystem documented in BleepingComputer reporting on EvilTokens and ARToken kits that bypass MFA by proxying legitimate M365 OAuth sessions in real time.
- **Anubis Ransomware Kill Chain Detector (CVE-2025-5777 / BYOVD / Supply Chain)** (Python) *(draft)* — Scans plain-text log exports for indicators of the Anubis ransomware kill chain across three stages: Citrix Bleed 2 (CVE-2025-5777) session-token extraction against NetScaler ADC/Gateway endpoints, BYOVD kernel driver abuse for defense evasion, and supply chain credential harvesting targeting CI/CD pipelines and package-registry auth stores. Each match is labeled by kill-chain stage with severity, source identifier, and the verbatim log line that triggered it.
- **Avalon Multi-Stage Phishing Chain & Lateral Movement Detector** (Python) *(draft)* — Scans plain-text proxy, SMTP gateway, or endpoint log exports for indicators of Avalon's documented multi-stage phishing intrusion chain across three kill-chain stages — initial spearphishing delivery and malicious document staging, payload execution and C2 beacon establishment via scripting engine abuse, and lateral movement through credential replay and internal service pivoting. Each finding is labeled with the corresponding MITRE ATT&CK technique ID to support triage and purple-team coverage gap analysis.
- **BitM Phish Detector** (Python) *(draft)* — Scans plain-text web proxy or access logs for browser-in-the-middle phishing infrastructure indicators, including reverse-proxy TLS header anomalies, session-cookie relay signatures, and Bluekit-style hostname clustering patterns. Emits timestamped findings to stdout with confidence scoring and indicator type tagging.
- **CDN Hash Sentinel** (Python) *(draft)* — Simulates a CDN-level supply chain attack by generating a fake plugin update log with an injected hash mismatch, then scans the log to detect unexpected asset hash changes that indicate possible JavaScript tampering. Demonstrates how blue teams can monitor CDN asset integrity to catch compromised plugin update channels.
- **CUCM CVE-2026-20230 Triage Scanner** (Python) *(draft)* — Parses Cisco Unified Communications Manager plain-text audit and access logs for SSRF trigger patterns and privilege-escalation sequences linked to CVE-2026-20230. Emits severity-rated findings with matched line context to stdout for rapid analyst triage within the sub-24-hour weaponization window.
- **CVE-2026-20230 Cisco UCM Triage Scanner** (Python) *(draft)* — Scans plain-text logs for indicators of active exploitation of CVE-2026-20230 in Cisco Unified Communications Manager using pattern matching against known attack signatures. Outputs a structured triage report with matched IOCs, severity assessment, and patch verification guidance to stdout.
- **Check Point VPN Qilin Sigma Rule Generator** (Python) *(draft)* — Generates Sigma detection rules for the Check Point VPN auth-bypass exploitation chain (CVE-2024-24919) used by Qilin ransomware affiliates, mapping each attack stage to MITRE ATT&CK. Accepts plain-text VPN/auth logs and outputs ready-to-use Sigma YAML rules to stdout.
- **CitrixBleed NetScaler Memory-Disclosure & Session-Hijack Detector** (Python) *(draft)* — Scans plain-text HTTP access log exports from NetScaler ADC/Gateway appliances for CVE-2023-4966 CitrixBleed memory-disclosure attack signatures across three kill-chain stages — initial exploit attempts against vulnerable authentication endpoints producing anomalously oversized responses, session-hijack activity using stolen in-memory tokens, and post-exploitation Anubis ransomware staging and BYOVD delivery indicators tied to the CVE-2025-5777 supply-chain intrusion chain. Each finding is labeled with a MITRE ATT&CK technique ID to support immediate NetScaler defender triage.
- **ClickFix Loader Detector** (Python) *(draft)* — Scans plain text logs for behavioral indicators matching BabaDeda, Lorem Ipsum, and Potemkin loader patterns surfaced in ClickFix campaigns. Outputs YARA-style and Sigma-compatible rule matches to stdout with timestamps and severity ratings.
- **Commvault Ransomware Pre-Staging Detector** (Python) *(draft)* — Analyzes plain-text Commvault log exports for ransomware pre-staging indicators including anomalous API calls, credential enumeration patterns, and job-schedule tampering. Helps blue team analysts triage potential ransomware intrusions targeting backup infrastructure before encryption begins.
- **Container Escape & Privesc Detector** (Python) *(draft)* — Parses Linux audit and syslog plaintext output to detect exploitation signatures associated with kernel privilege-escalation and container breakout techniques (e.g., nf_tables UAF, runc/OverlayFS escapes, capability abuse). Includes a lab mode that emits synthetic exploit event sequences to validate detection coverage without a live environment.
- **DetectIncommingConnection** (Python) — Monitors and alerts on new incoming network connections in real time.
- **EDR Probe Detector** (Python) *(draft)* — Analyzes plain text security logs to identify automated AI-assisted EDR evasion probing patterns such as rapid enumeration, timing anomalies, and systematic API call sequences. Generates actionable Sigma and YARA rules to detect these reconnaissance behaviors before any payload is deployed.
- **FortiBleed Post-Exploitation Triage Scanner** (Python) *(draft)* — Scans plain-text web server or proxy access log exports for post-exploitation indicators following FortiBleed attacks — Fortinet credential and configuration exposure exploits — covering the full intrusion chain through Nextcloud zero-day abuse and Inc/Lynx ransomware pre-deployment staging. Detects three kill-chain stages — FortiGate/FortiManager exploitation and credential-exposure probes, Nextcloud unauthorized API access and bulk file-staging patterns, and Inc/Lynx ransomware lateral-movement and data-aggregation behaviors — each finding labeled with a MITRE ATT&CK technique ID.
- **FortiBleed Triage Scanner** (Python) *(draft)* — Scans plain-text FortiGate logs for exploitation indicators tied to the FortiBleed vulnerability cluster (CVE-2024-21762, CVE-2023-27997, CVE-2024-23113), correlating suspicious SSL-VPN requests, auth-bypass patterns, and post-exploitation beaconing. Outputs a prioritized hit list with source IPs, matched CVE tags, and a remediation checklist for triaging exposed devices.
- **FortiGate VPN Credential Dump Reuse & Inc/Lynx Staging Detector** (Python) *(draft)* — Scans plain-text FortiGate VPN authentication log exports for credential dump reuse patterns — where accounts exposed via FortiBleed are replayed against active VPN gateways — correlating authentication anomalies such as credential-stuffing bursts, off-hours logins, and geolocation shifts with Inc/Lynx pre-ransomware staging behaviors. Covers three kill-chain stages — FortiBleed-exposed credential replay and authentication-success-after-failure sequences, VPN authentication anomaly clusters tied to known threat-actor source ranges, and Inc/Lynx lateral-movement and session-persistence indicators — each finding labeled with a MITRE ATT&CK technique ID.
- **Gamaredon TTP Log Scanner** (Python) *(draft)* — Scans plain-text logs against a curated ruleset of Gamaredon (Shuckworm/Armageddon) TTPs, targeting their updated malware-loading chains — PowerShell loaders, LOLBin abuse, LNK/HTA droppers — and infrastructure-obfuscation patterns such as dynamic-DNS C2, Telegram bot channels, and fast-flux indicators. Emits per-match findings with TTP label, severity, and source line to stdout for immediate triage or pipeline consumption.
- **Gravity SMTP Secret Exposure Scanner** (Python) *(draft)* — Scans plaintext web server access logs for request patterns indicative of unauthenticated exploitation of the Gravity SMTP plugin's info disclosure vulnerability, matching against a Sigma-style detection rule. Outputs timestamped findings to stdout with severity, matched indicators, and source IPs to support triage of active exploitation attempts against WordPress deployments.
- **Langflow Agentic Tool-Call Abuse & Lateral Movement Detector** (Python) *(draft)* — Scans plain-text Langflow or LLM orchestration framework execution logs for agentic attack patterns across three kill-chain stages — tool-call abuse where LLM reasoning chains invoke high-risk capabilities such as shell execution or credential file access, lateral movement where agents pivot across flows or memory stores via injected reasoning steps, and exfiltration via LLM-mediated outbound tool invocations or prompt-encoded data egress. Targets the emerging attack surface of autonomous agent frameworks where compromised or injected reasoning steps can weaponize legitimate tool nodes against internal infrastructure.
- **Langflow CVE-2026-5027 Triage Scanner** (Python) *(draft)* — Detects exploitation attempts and post-exploitation artifacts of the Langflow unauthenticated file-write RCE vulnerability (CVE-2026-5027). Scans plain-text log files for suspicious endpoint hits, file-write payloads, and known post-exploitation indicators on AI development hosts.
- **LiteLLM SSRF-to-RCE Detector** (Python) *(draft)* — Scans LiteLLM and AI gateway plain-text logs for SSRF-to-RCE exploit chain indicators using regex-based IOC matching against known attack patterns targeting the /config/update endpoint, internal callback URIs, and metadata service probes. Outputs timestamped flagged lines with severity tags and a prioritized hardening checklist for LiteLLM deployments.
- **Miasma Rule Generator** (Python) *(draft)* — Scans plain-text logs for Miasma malware IOCs and behavioral signatures tied to its credential-harvesting and supply-chain delivery mechanics. Emits ready-to-deploy YARA or Sigma rules to stdout based on matched evidence.
- **Netlogon RCE Detector** (Python) *(draft)* — Detects CVE-2026-41089 exploitation attempts by scanning plain-text Windows Security event logs for suspicious Event ID 4742/5805 patterns and anomalous Netlogon RPC signatures. Alerts on credential resets, unauthorized secure channel establishment, and RPC anomalies indicative of privilege escalation via Netlogon.
- **Oracle EBS CVE-2026-46817 Exploitation Detector** (Python) *(draft)* — Scans plain-text Oracle HTTP Server or Apache combined-format access log exports for active exploitation of CVE-2026-46817 against Oracle E-Business Suite, prioritized for financial-sector SOC teams. Detects three kill-chain stages — reconnaissance probing of vulnerable EBS servlet endpoints, authentication-bypass exploitation payloads in request URIs, and post-exploitation IOCs including attacker IPs and exfiltration domains observed in financial-sector campaigns.
- **PamStealer macOS PAM Abuse & AppleScript Detector** (Python) *(draft)* — Scans macOS unified log exports for PamStealer campaign indicators, detecting PAM module injection into /etc/pam.d/ authorization stacks and compiled AppleScript execution via osascript or osacompile launched from unsigned or path-impersonating binaries. Covers three kill-chain stages — PAM module staging, privileged credential interception, and AppleScript-based execution or exfiltration — each labeled with a MITRE ATT&CK technique ID.
- **SEO Poison DNS Detector** (Python) *(draft)* — Parses plain-text DNS or proxy logs to flag domains matching indicators associated with SEO-poisoned fake open-source package sites delivering Remus Stealer and SessionGate via traffic distribution systems. Outputs timestamped alerts to stdout and optionally appends flagged IPs to a blocklist file compatible with DetectIncomingConnectionandBlockIP.
- **SharePoint CVE-2026-45659 RCE Post-Exploitation Detector** (Python) *(draft)* — Scans plain-text IIS W3C or Apache combined-format access log exports for post-exploitation indicators following successful CVE-2026-45659 SharePoint RCE, a CISA KEV-listed critical vulnerability under active exploitation. Detects three kill-chain stages — initial RCE trigger attempts against vulnerable SharePoint server-side processing endpoints, web shell deployment and interactive access patterns in SharePoint layout and catalog paths, and post-exploitation activity including credential staging URI patterns, lateral movement beacons, and known campaign C2 IOCs.
- **SimpleHelp CVE-2026-48558 & Djinn Cloud/AI Credential Exfil Detector** (Python) *(draft)* — Scans plain-text HTTP access or SIEM log exports for active exploitation of CVE-2026-48558 against SimpleHelp remote-support instances combined with credential-exfiltration patterns exhibited by the Djinn stealer, which specifically targets cloud-provider credential files and AI API keys. Covers three kill-chain stages — SimpleHelp exploitation probes and authentication-bypass attempts, Djinn stealer access to cloud and AI credential stores, and outbound C2 transmissions to known Djinn infrastructure.
- **Splunk Unauth File-Op Triage** (Python) *(draft)* — Scans plain-text Splunk Enterprise access logs for unauthenticated file-operation attempts matching CVE-2026-20253 exploit patterns, then cross-checks a supplied version string to confirm whether the targeted instance carries the patch. Produces a timestamped stdout report suitable for SOC triage or portfolio evidence.
- **ToddyCat Umbrij Gmail OAuth Token Abuse Detector** (Python) *(draft)* — Scans Google Workspace audit log exports for ToddyCat Umbrij-style OAuth token abuse patterns, flagging service-account API calls carrying gmail.readonly, mail.google.com, or gmail.modify scopes that fall outside expected delegated-access baselines. Covers three kill-chain stages — OAuth scope grant anomalies where service accounts receive unexpected Gmail scopes, mailbox access where service-account tokens are exercised against individual user mailboxes, and bulk enumeration indicative of ToddyCat's systematic mail collection.
- **Voice Assistant Injection Detector** (Python) *(draft)* — Simulates notification-borne prompt injection payloads to model how malicious content in alerts or messages can hijack AI voice assistant behavior. Analyzes plain-text assistant action logs for heuristic anomalies indicating the assistant may have acted on injected instructions rather than legitimate user commands.
- **WinRE XML BitLocker Bypass Detector** (Python) *(draft)* — Simulates the GreatXML BitLocker bypass technique by generating synthetic WinRE recovery XML access event sequences, then scans plain-text Windows logs for the same anomalous ReAgent.xml and BCD file access signatures. Outputs flagged log lines and a ready-to-import Sigma or Elastic EQL detection rule to stdout.
- **Woodgnat/Mistic Kill Chain Detector** (Python) *(draft)* — Scans plain-text endpoint log exports for Woodgnat/Mistic campaign indicators: photo-themed ZIP phishing delivering Node.js implants to hospitality-sector front-desk machines. Detects process-spawn anomalies (node.exe from non-developer parents), image-named archive extraction events, and hardcoded Mistic C2 network IOCs across three kill-chain stages.
- **Woodgnat/Mistic Native Messaging Kill Chain Detector** (Python) *(draft)* — Scans plain-text process/event logs for Native Messaging host invocations spawned by browser extension processes, a staging technique observed in the Woodgnat/Mistic ransomware kill chain. Emits timestamped, severity-ranked alerts to stdout when suspicious parent-child process chains or messaging manifest paths match known-bad patterns.

### Forensics
- **C/C++ Taint Flow Scanner** (Python) *(draft)* — Performs pattern-based taint analysis on C/C++ source files by tracing untrusted input from sources (stdin, argv, fgets) to dangerous sinks (strcpy, system, sprintf). Outputs structured bug reports with file location, taint path, and a reproduction stub to stdout, suitable for embedding in CI pipelines.
- **Fake CVE PoC RAT Scanner** (Python) *(draft)* — Scans a locally cloned GitHub repository for ChocoPoC-style RAT delivery indicators embedded in fake CVE proof-of-concept repos that target security researchers. Detects three attack stages: CVE-impersonation lures in README and metadata, payload staging in GitHub Actions workflows, and RAT installation patterns including base64 decode chains, Discord or Telegram C2 callbacks, and OS persistence commands.
- **HARanalyzer** (Python) — Parses and analyzes HTTP Archive (HAR) files to surface security-relevant requests, headers, and responses.
- **OSS Secret Scanner** (Python) *(draft)* — Scans plain-text source code dumps or CI pipeline log output for leaked credentials using stdlib regex patterns, classifying each finding by blast radius (high/medium/low) based on credential type and revocability. Outputs a triage-ready report to stdout designed for direct integration into CI pass/fail gates.
- **Pwn Request Guard** (Python) *(draft)* — Scans GitHub Actions workflow YAML files for the Cordyceps pattern, where untrusted external input such as pull request titles, branch names, or issue bodies flows into privileged execution contexts like write-permission jobs or environment variable interpolations. Outputs flagged findings with file, line number, and a plain-text risk summary to stdout.
- **list IPs in a pcap** (Python) — Extracts and lists all unique IP addresses observed in a PCAP capture file.
- **list IPs in a pcap with HTTP code NOT 200** (Python) — Extracts IPs from a PCAP that generated non-200 HTTP responses — useful for spotting errors, probes, and attacks.

### IncidentResponse
- **DetectIncommingConnectionandBlockIP** (Python) — Detects suspicious incoming connections and automatically blocks the source IP via firewall rules.

### Labs
- **AI Agent Human-in-the-Loop Checkpoint Lab** (Python) *(draft)* — Interactive CLI lab that simulates an AI security agent executing tasks with explicit human approval gates at privilege escalation and data access decision points. Demonstrates how mandatory checkpoints prevent automated exploitation by requiring operator sign-off before sensitive operations proceed.
- **AI IDE Prompt Injection Escape Lab** (Python) *(draft)* — Simulates and detects zero-click prompt injection sandbox escapes in AI-powered IDEs by scanning endpoint process logs for anomalous child-process lineage where editor processes (VS Code, Cursor, Windsurf) spawn unexpected shell interpreters or credential-harvesting commands. Pairs each finding with the responsible injection technique and a concrete mitigation to build hands-on defensive intuition.
- **Agentjacking Prompt Injection Lab** (Python) *(draft)* — Simulates and detects agentjacking attacks where clean-looking GitHub repositories embed hidden prompt-injection payloads targeting AI coding agents during repo setup. Scans a local repository directory for Unicode steganography, AI-targeting instruction files, and encoded payloads in setup scripts, then pairs each finding with a concrete mitigation.
- **Chrome Extension AI-Impersonation Behavioral Lab** (Python) *(draft)* — Scans an unpacked Chrome extension directory for behavioral indicators of AI-impersonation: address-bar keylogging via omnibox/input event listeners and search-query proxying to non-vendor endpoints. Pairs each finding with the specific permission or code pattern exploited and a concrete mitigation to build defensive intuition.
- **ConsentFix & ClickFix OAuth Token Theft Lab** (Python) *(draft)* — Simulates and detects ConsentFix and ClickFix OAuth token theft campaigns against M365 tenants by scanning plain-text Unified Audit Log exports for anomalous consent grants and stolen-token replay indicators. Pairs each finding with the responsible social-engineering technique and a concrete mitigation to build hands-on defensive intuition.
- **EDR Kill Chain Lab** (Python) *(draft)* — Replays a macOS non-admin EDR-disable chain by printing ordered TTP steps with expected telemetry signatures, then scans a plain-text unified log export for the same indicator sequence to validate detection coverage. Designed as a blue-team training tool that teaches both the attack pattern and the log queries that catch it.
- **LLM Security Benchmark** (Python) *(draft)* — Scores LLM-generated responses against a fixed rubric of security research tasks including CVE triage, exploit summarization, and malware prompt analysis. Outputs a ranked scorecard to stdout so practitioners can compare models on domain-specific accuracy.
- **LLM vs Hybrid Vulnerability Scanner Benchmark** (Python) *(draft)* — Scans a corpus of plain-text source files with two pipelines — a broad LLM-style heuristic scanner and a hybrid pipeline that layers separation-logic-inspired context filters on top — to benchmark false-positive rates between the two approaches. Computes and reports per-pipeline precision and false-positive rate against an optional ground-truth file, giving security researchers a reproducible baseline for comparing pure-LLM and logic-constrained vulnerability detection strategies.
- **LUKS Dead Man's Switch Lab** (Python) *(draft)* — Simulates and audits LUKS dead man's switch configurations by replaying plain-text system log events to detect USB-absence trigger conditions and timer-based lockout windows, pairing each finding with a concrete hardening recommendation. Demonstrates how endpoint full-disk encryption can be configured to auto-lock when a trusted USB token is removed or a heartbeat timer expires, teaching the gap between intended and actual trigger coverage.
- **npm Supply Chain Attack Lab** (Python) *(draft)* — Parses a plain-text npm verbose install log to replay a classic postinstall-script supply chain attack, then re-evaluates each detected script under npm 12's opt-in execution model to show exactly what gets blocked. Outputs a side-by-side comparison table and a ready-to-paste allowlist for packages whose postinstall scripts are legitimate.
- **npm Supply Chain Integrity Lab** (Python) *(draft)* — Simulates the DPRK Mastra npm attack pattern by injecting synthetic malicious transitive dependencies into a package-lock.json manifest, then applies regex IOC rules to detect crypto wallet extension targeting via postinstall hooks and typosquatted package names. Outputs a plain-text findings table with severity ratings and MITRE ATT&CK T1195.001 tagging to stdout.

### Posture
- **CI/CD Permission & Token Exposure Scanner** (Python) *(draft)* — Scans plain-text CI/CD configuration files and pipeline logs for overly permissive scope declarations and exposed credential patterns that enable unauthenticated repository hijacking. Produces a prioritized risk report to stdout with line-level references and remediation guidance.
- **DownloadDNSRecordsInCSVforADomain** (Python) — Exports all DNS records for a given domain to a CSV file for inventory and monitoring.
- **EDR Telemetry Gap Detector** (Python) *(draft)* — Parses plain-text EDR/syslog output to identify coverage gaps exploitable by AI-assisted evasion techniques and lateral movement via AD discovery. Flags missing telemetry signals that would leave container escapes and privilege escalation undetected.
- **Healthcheck** (PowerShell) — System health check script covering key security-relevant services and configurations.
- **Healthcheck (Python)** (Python) — Python version of the system health check for Linux/cross-platform environments.
- **MCP Config Scanner** (Python) *(draft)* — Scans MCP server configuration files for known misconfigurations and supply-chain risks using customizable detection rules. Outputs structured findings to stdout with severity ratings and remediation hints.
- **findIdP** (Python) — Identifies the Identity Provider (IdP) for a given domain or email address.

### SupplyChain
- **Browser Extension Supply Chain Scanner** (Python) *(draft)* — Statically analyzes installed browser extension source files to detect dormant or obfuscated script-injection capabilities consistent with supply chain compromise. Uses the Adblock for YouTube attack as the reference threat model, flagging eval chains, remote code fetching, obfuscation patterns, and over-privileged manifest permissions.
- **Contagious Interview NPM Payload Scanner** (Python) *(draft)* — Detects behavioral signatures of Contagious Interview threat actor TTPs in npm package audit logs and repository metadata, including malicious install hooks, staged payload delivery patterns, and trojanized developer-tool indicators. Outputs prioritized alerts to stdout for integration into CI/CD pipeline log monitoring workflows.
- **Marketplace Plugin Risk Scanner** (Python) *(draft)* — Scans a plain-text log of IDE plugin metadata (publisher age, download counts, timestamps) to flag suspicious packages using velocity anomalies and publisher freshness heuristics. Outputs risk-scored findings to stdout for use in CI/CD pre-install gates.
- **Marketplace Skill Risk Scanner** (Python) *(draft)* — Statically analyzes AI agent skill and plugin source files for behavioral indicators of malicious activity that evade automated marketplace security scans. Scores each file against a weighted rubric of AIR-documented patterns including prompt injection hooks, covert exfiltration calls, and permission escalation attempts.
- **Miasma/Hades Supply Chain Sentinel** (Python) *(draft)* — Scans plain-text npm package metadata and Go module logs for Miasma/Hades malware-family indicators using heuristic risk scoring of package fields, dependency patterns, and GitHub Actions workflow anomalies. Emits scored, line-per-finding results to stdout so findings can be piped into any alerting pipeline.
- **NPM IOC Log Scanner** (Python) *(draft)* — Scans plain-text CI/CD and npm audit logs for TeamPCP supply chain campaign indicators using embedded YARA-style pattern rules. Detects malicious package names, suspicious install hooks, and known IOC strings associated with npm-based supply chain attacks.
- **NPM Malicious Pattern Scanner** (Python) *(draft)* — Scans npm install or audit log output for behavioral patterns associated with worm-style credential stealers, as documented in the Red Hat supply-chain campaign. Flags suspect package events to stdout so CI/CD pipelines can gate on exit code.
- **NPM Sleet Scanner** (Python) *(draft)* — Scans plain-text npm install logs for Sapphire Sleet supply chain compromise indicators including typosquatted Mastra AI package names, post-install script network telemetry callbacks, and base64-encoded payload execution patterns. Extends the Contagious Interview NPM Payload Scanner ruleset with DPRK-attributed TTPs observed in the 2024–2025 Mastra AI campaign.
- **PolinRider Package Registry Threat Detector** (Python) *(draft)* — Scans a locally downloaded package directory for PolinRider-campaign TTPs across npm, Go, and Packagist registries, flagging typosquatting and dependency-confusion naming patterns, metadata anomalies such as version-number abuse and missing maintainer fields, and behavioral indicators in install scripts including base64 decode chains and environment-variable exfiltration. Emits one labeled alert line per detection, enabling supply-chain analysts to triage suspicious packages before installation or CI ingestion.
- **PyPI Trojan Scanner** (Python) *(draft)* — Downloads a PyPI package wheel and scans its extracted Python source files for credential-harvesting stub patterns characteristic of supply-chain trojanization attacks. Reports suspicious code fragments, matched pattern names, and line numbers to stdout without executing any package code.
- **SRI Drift Monitor** (Python) *(draft)* — Parses plain-text web server or CDN access logs to detect when third-party script URLs return content whose hash no longer matches an expected SRI baseline, flagging the drift pattern seen in supply-chain injection attacks like the Polymarket incident. Alerts are written to stdout with timestamp, URL, expected hash, and observed hash for immediate triage.
- **VS Code Task Lifecycle Infostealer Detector** (Python) *(draft)* — Scans plain-text endpoint logs for the supply-chain campaign that plants .vscode/tasks.json inside hijacked npm/Go packages to bypass postinstall-script restrictions and drop a Python infostealer. Detects three kill-chain stages: npm spawning VS Code during package install, VS Code tasks invoking python against credential stores, and outbound connections to campaign-linked exfiltration domains.
- **WordPress Plugin Integrity Scanner** (Python) *(draft)* — Computes SHA256 hashes of installed WordPress plugin files and diffs them against the official WordPress.org SVN repository to detect unauthorized modifications. Surfaces injected backdoor code from supply chain tampering by flagging files that have been added, removed, or altered since the official release.
- **npm Typosquat & Impersonator Detection Lab** (Python) *(draft)* — Scans a package.json or plain-text npm install log for typosquatted and impersonator packages by computing bigram-overlap name-similarity scores against a curated popular-package baseline, then inspects lifecycle scripts for outbound network callbacks and credential-harvesting patterns. Designed to run as a CI/CD gate that surfaces suspicious dependency additions before they reach production.

### ThreatHunting
- **CVE Triage Scanner** (Python) *(draft)* — Parses plain-text CVE feed logs to score exploitability based on keyword heuristics and CVSS indicators, then drafts Sigma detection rules for high-severity findings. Outputs triage results and detection rules to stdout without requiring external dependencies or human prompting.
- **FortiSandbox Post-Exploitation Scanner** (Python) *(draft)* — Applies embedded Sigma-style detection rules to plain text FortiSandbox/FortiGate logs to surface post-exploitation indicators consistent with the 30,000-credential compromise baseline. Outputs severity-ranked matches to stdout for triage without requiring external tooling or SIEM integration.
- **Ivanti Sentry Sigma Rule Matcher** (Python) *(draft)* — Scans plain-text log files for attacker pre-positioning patterns associated with post-disclosure exploitation of Ivanti Sentry edge appliances. Matches log lines against a built-in Sigma-style rule set covering CVE-2023-38035 and related recon/exploitation indicators, printing structured runbook alerts to stdout.
- **PeopleSoft CVE-2026-35273 Hunt Rule Generator** (Python) *(draft)* — Scans plain-text PeopleSoft application logs for CVE-2026-35273 exploitation indicators attributed to ShinyHunters TTPs and emits Sigma-compatible detection rules to stdout. Extends the WebLogic CVE-2024-21182 hunt rule pattern with PeopleSoft-specific URI traversal, credential-stuffing, and bulk-export signatures.
- **RedCAP Long-Dwell Credential Theft Detector** (Python) *(draft)* — Analyzes plain-text authentication logs for behavioral patterns consistent with UNC6508-style credential theft campaigns targeting research institutions, including low-and-slow login enumeration, off-hours access spikes, and credential reuse across research portals. Outputs a prioritized alert report to stdout with dwell-time estimates and MITRE ATT&CK mappings.
- **Tailscale+SSH Persistence Detector** (Python) *(draft)* — Parses plain-text system logs to detect co-installation of Tailscale and OpenSSH on endpoints, flagging the combination as a potential covert persistence indicator. Outputs structured Sigma-style alert findings to stdout for triage or pipeline ingestion.
- **VS Code URI Token Hunter** (Python) *(draft)* — Scans plain-text CI/CD pipeline logs for indicators of GitHub token exfiltration via VS Code workspace trust abuse, flagging suspicious URI handler invocations, malicious .vscode/settings.json patterns, and credential exposure sequences. Outputs timestamped findings with severity and matched rule to stdout for rapid triage by pipeline defenders.
- **WebLogic CVE-2024-21182 Hunt Rule Generator** (Python) *(draft)* — Parses plain-text HTTP/WebLogic access and server logs to detect exploitation patterns matching CVE-2024-21182 (JNDI/T3 deserialization abuse) as tracked in active KEV campaigns. Outputs structured Sigma-style detection hits with timestamp, source IP, and matched pattern to stdout.
- **Webshell IOC Forge** (Python) *(draft)* — Scans plain-text web server access logs for obfuscation patterns and post-drop behaviors associated with modern webshell variants catalogued in SANS ISC diaries. Emits ranked YARA string/condition stubs and Sigma detection/condition YAML stubs for the highest-confidence indicator clusters found.
- **Xeno RAT Log Hunter** (Python) *(draft)* — Scans plain-text log files for Xeno RAT indicators including C2 registration patterns, persistence registry keys, and network beaconing signatures. Outputs timestamped SIGMA-aligned findings to stdout, tuned for government finance sector environments.

## RedTeam

### Labs
- **AWS Privilege Escalation Probe** (Python) *(draft)* — Simulates IAM privilege-escalation and S3 misconfiguration scenarios against a LocalStack endpoint so practitioners can learn and test detection logic offline. Outputs a structured audit trail to stdout showing each attack path attempted and whether it succeeded.
- **PhishBench** (Python) *(draft)* — Fires configurable prompt-injection phishing payloads at AI email agent endpoints and measures resilience by parsing stdout responses for compliance or refusal signals. Benchmarks multiple model/config profiles in a single run and reports a scored summary to stdout.
- **Prompt Injection Lab** (Python) *(draft)* — Demonstrates prompt injection and data exfiltration attacks against a simulated RAG pipeline, then shows how zero-trust input/output validation mitigations block each attack vector. Runs attack scenarios end-to-end in a single script with annotated stdout output for hands-on learning.
- **Vulnerability Triage Lab** (Python) *(draft)* — Scans a deliberately vulnerable Python codebase for common vulnerability patterns (SQLi, command injection, hardcoded secrets, path traversal) and produces a prioritized triage report. Guides practitioners through reading findings and understanding why each pattern is dangerous.

- **PingScan** (Shell) — ICMP ping sweep to discover live hosts on a network range.
- **SearchKeyWordsInAWebsite** (Python) — Crawls a website and searches all pages for specified keywords — useful for recon and content discovery.
- **SearchKeyWordsInAWebsiteThroughTor** (Python) — Same as SearchKeyWordsInAWebsite but routes traffic through the Tor network for anonymity.
- **TelnetPortScanner** (Shell) — Port scanner using telnet to test connectivity to common service ports.
- **TestFiletypeuploads** (Python) — Tests file upload endpoints for weak extension/MIME type restrictions that could allow malicious uploads.

## GRC

- **Agent Policy Auditor** (Python) *(draft)* — Parses plain-text agent audit logs to detect over-privileged access patterns when autonomous agents interact with Postgres and Kubernetes. Flags violations of least-privilege policy rules and reports findings to stdout.
- **CVE Asset Correlator** (Python) *(draft)* — Ingests a CVE list and a plain-text SBOM/CMDB asset inventory, correlates vulnerabilities against affected assets, and outputs prioritized remediation tickets to stdout. Ranks findings by CVSS severity and asset criticality to surface the highest-risk exposures first.
- **GRC Evidence Gap Scanner** (Python) *(draft)* — Parses plain-text audit logs against a JSON-defined control catalog and surfaces controls whose evidence is missing or outside the required collection window. Outputs a prioritized remediation list to stdout, simulating the ticket-opening step a full agent would automate.
- **UploadExceltoSharepointList** (PowerShell) — Uploads data from an Excel file into a SharePoint list for compliance tracking and reporting.
- **UploadExceltoSharepointListUsingPrimaryKey** (PowerShell) — Uploads Excel data to SharePoint using a primary key to prevent duplicate entries.
- **checkHostingPlatform** (Python) — Identifies the hosting platform (AWS, Azure, GCP, etc.) for a given domain or IP.
- **dnsinventory** (Python) — Enumerates and inventories DNS records across multiple domains for asset management and compliance.
